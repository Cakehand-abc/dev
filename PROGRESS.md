# 项目进度与下次继续入口

更新：2026-09-08。下次使用时先向用户说明本文件中的已完成功能，再继续未完成事项。

## 已完成代码

Spring Boot 3.5.16、Java 17 编译目标、Maven Wrapper 3.9.16、MyBatis-Plus 3.5.17 与 MyBatis XML、Vue 3、ECharts 6。实现登录与退出、首页、人口统计、健康统计、医生随访、腕表分布、圆形围栏、轨迹播放，并配套老人、医生、设备、账号维护。三角色权限、CSRF、电话脱敏、随访唯一完成、绑定历史、乱序定位、越界去重与人工处理已实现。

## 已验证

- Maven 23 项测试通过，本机 JVM 为 Zulu 25.0.1，release 17；未实测 JDK 17 或 IDEA 内置运行器。
- MySQL 8.0.46 隔离库 16 张表，实际 HTTP 冒烟 16 项通过。
- Chrome 浏览器检查 12 项通过。
- 101080 健康记录、100648 定位记录，20 并发约 60 秒，2664 请求 0 错误；四端点 P95 均 < 2 秒，测试专属数据已清理。
- 代码已可打包运行，本机演示地址 127.0.0.1:18080，私有账号配置在 .local/demo-access.txt；不要把凭据写进公开文档。

## 当前交付状态

- 前后端代码、SQL 结构和独立虚构种子完成；09:10 最终 JAR 构建成功，已包含窄屏详情修复。
- 十份 Word 共 62 页和答辩 PPT 共 16 页已生成并逐页渲染检查。PPT 包完整性、尺寸、字体、可编辑表格和图表检查通过。
- 根 README、docs/README.md、完整接口目录和部署环境说明已完成。
- 最新 JAR 的 HTTP 16 项、浏览器 12 项再次通过，另补充窄屏设备详情验证。
- 独立 SQL 首次导入及非空表重复导入拒绝验证通过，测试专属表清理完成。
- 可转交组员的整套交付包在 release/smart-care-v1.0.zip，含源码、SQL、文档、测试证据和可执行 JAR，不含私有凭据与依赖缓存。

## 下次先说明的已完成功能

已实现登录退出、首页、人口信息分析、健康检测分析、医生随访分析、腕表分布、电子围栏、轨迹回放八项功能，并附老人、医生、设备和账号管理。三角色、历史绑定、越界告警和回放交互已接入真实后端与 MySQL。

## 留待团队补充

成员姓名与学号、实际例会信息、完整 IDEA 版本及人工验收签字。JDK 17 和 IDEA 内置 JUnit 未实测；当前验证 JVM 为 25、release 17。公开部署、真实设备、道路地图和备份恢复演练未执行。无需再次选择模板。

## 用户约定

不设独立 Agent 岗位；四人 A 经理测试、B 后端、C 后端数据、D 前端。使用虚构数据与无道路底图的离线坐标画布。第 8 项文档改详细设计，第 11 项为部署环境说明。成员姓名、学号、IDEA 完整版本与截止日期待补充。

用户 2026-09-08 要求持续工作，若额度耗尽或需要选择模板样式则先保存进度并关机。当前模板已选，不需再问。额度耗尽可能直接阻断工具执行，不能保证自动关机；已向用户说明此限制。每阶段更新此文件。不得因本文件自行定时关机或在未出现条件时关机。

## 本机验证环境

隔离 MySQL 端口 13316，数据位于 TEMP/smart-care-mysql-01a07b9c/data。可运行 JAR 位于 target/dev-0.0.1-SNAPSHOT.jar。避免停止用户已有 8080 服务或其他 java/mysql 进程。重新打包前先停止本项目 JAR，否则 Windows 文件占用会阻止 repackage。

Word 与 PPT 模板已经选定。Word 使用 System Design；渲染使用经用户授权下载解压到 TEMP/smart-care-render-01a07b9c/extracted/program 的 LibreOffice，不是正常安装。任务脚本和 QA 在 scripts 与 docs/.qa。最终交付在 docs/deliverables，QA 中间文件不交付。
