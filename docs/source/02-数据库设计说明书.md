# 数据库设计说明书
版本 V1.1　2026年9月9日

## 1 数据库总体架构与设计原则

智慧医养大数据平台底层采用 MySQL 8.0 关系型数据库系统构建，存储引擎采用高可靠的 InnoDB 引擎。全库严格遵循第三范式（3NF）设计原则，并针对大数据量读写与多维时空分析场景进行了适度反范式与多维联合索引优化。数据库字符集统一采用 utf8mb4，排序规则为 utf8mb4_unicode_ci，全面杜绝特殊生僻字与表情符号存储异常；时区严格对齐 Asia/Shanghai。

| 业务功能域 | 包含数据表与核心职责 |
| --- | --- |
| 身份与组织架构 | sys_user（系统账号）、region（网格区域）、elder（老人基础档案）、doctor（家庭医生） |
| 健康监测与随访 | health_rule（阈值规则）、health_record（健康流水）、followup_plan（随访计划）、followup_record（随访结果） |
| 设备资产与接入 | watch_device（腕表设备）、device_credential（安全凭据）、device_heartbeat_event（在线心跳）、device_binding（绑定历史） |
| 时空定位与围栏 | location_point（时序定位）、geofence（电子围栏）、geofence_member（纳管成员）、geofence_state（监测状态）、geofence_alert（越界告警） |
| 安全审计与追溯 | audit_log（用户操作与敏感变更安全审计日志） |

## 2 实体关联与数据生命周期设计

1. 强弱实体与级联关系：行政网格一对多老人；老人一对多体检流水与随访计划；医生一对多随访计划。老人建档状态分为在册（ACTIVE）与归档（ARCHIVED），业务层采用软归档机制，确保已有历史体检流水与随访记录的法律凭证有效性与可追溯性。

2. 穿戴设备解耦与历史追溯：老人与智能腕表通过 device_binding 历史关联表进行物理隔离与时间线解耦。每条绑定记录包含 bound_at 与 unbound_at 时间区间，历史定位流水保留发生时的 binding_id 与老人主键；老人换绑腕表后，历史轨迹责任清晰，绝不发生数据污染。

3. 围栏监测状态机与循环外键解耦：geofence_state 记录当前最新监测快照，geofence_alert 记录越界历史事件。建表脚本先分别建立两表并定义常规外键，最后通过 ALTER TABLE 追加 active_alert_id 约束，完美化解实体间的循环外键依赖。

4. 主键与数值精度策略：全表业务主键统一采用 BIGINT UNSIGNED 自增主键，后端向前端返回 JSON 时，通过 Jackson 将 Long 型数值序列化为 String 字符串，彻底规避 JavaScript 64 位双精度浮点数造成的最后两位精度截断问题。

## 3 系统管理员与操作员账号表（sys_user）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| username | VARCHAR(64) | 必填 唯一  | 登录账号 |
| password_hash | VARCHAR(100) | 必填  | BCrypt加密密文 |
| display_name | VARCHAR(80) | 必填  | 用户显示姓名 |
| role | VARCHAR(20) | 必填  | 角色标识(ADMIN/OPERATOR/ANALYST) |
| enabled | BOOLEAN | 必填 默认 TRUE | 账号启用状态 |
| auth_version | INT | 必填 默认 0 | 会话认证版本号 |

表级完整性约束：CONSTRAINT ck_user_role CHECK (role IN ('ADMIN','OPERATOR','ANALYST'))

## 4 行政区域与网格划分表（region）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| code | VARCHAR(30) | 必填 唯一  | 业务编码 |
| name | VARCHAR(80) | 必填  | 名称 |
| parent_id | BIGINT | 可空  | 父级地区主键 |

表级完整性约束：FOREIGN KEY (parent_id) REFERENCES region(id)

## 5 老人基础健康档案表（elder）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| code | VARCHAR(40) | 必填 唯一  | 业务编码 |
| name | VARCHAR(80) | 必填  | 名称 |
| gender | VARCHAR(10) | 必填  | 性别标识 |
| birth_date | DATE | 可空  | 出生日期 |
| region_id | BIGINT | 必填  | 所属网格主键 |
| phone | VARCHAR(30) | 可空  | 联系电话(脱敏) |
| status | VARCHAR(20) | 必填 默认 'ACTIVE' | 在册业务状态 |
| version | INT | 必填 默认 0 | 乐观锁并发版本号 |

表级完整性约束：FOREIGN KEY (region_id) REFERENCES region(id)；CHECK (gender IN ('MALE','FEMALE','UNKNOWN'))；CHECK (status IN ('ACTIVE','ARCHIVED'))

性能联合索引：ix_elder_region (region_id,status)

## 6 签约家庭医生表（doctor）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| code | VARCHAR(40) | 必填 唯一  | 业务编码 |
| name | VARCHAR(80) | 必填  | 名称 |
| department | VARCHAR(100) | 必填  | 科室分类 |
| phone | VARCHAR(30) | 可空  | 联系电话(脱敏) |
| enabled | BOOLEAN | 必填 默认 TRUE | 账号启用状态 |
| version | INT | 必填 默认 0 | 乐观锁并发版本号 |

## 7 健康指标阈值判定规则表（health_rule）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| metric | VARCHAR(30) | 必填  | 指标名 |
| lower_bound | DOUBLE | 必填  | 参考下界 |
| upper_bound | DOUBLE | 必填  | 参考上界 |
| unit | VARCHAR(20) | 必填  | 物理单位 |
| version | INT | 必填  | 规则版本 |
| enabled | BOOLEAN | 必填  | 账号启用状态 |

表级完整性约束：UNIQUE(metric,version)；CHECK (lower_bound < upper_bound)

## 8 老人体检健康检测记录表（health_record）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| measured_at | DATETIME(6) | 必填  | 体检时间 |
| systolic | DOUBLE | 可空  | 收缩压(mmHg) |
| diastolic | DOUBLE | 可空  | 舒张压(mmHg) |
| heart_rate | DOUBLE | 可空  | 心率(次/分) |
| oxygen | DOUBLE | 可空  | 血氧饱和度(%) |
| temperature | DOUBLE | 可空  | 体温(℃) |
| abnormal | BOOLEAN | 必填  | 是否超标标识 |
| rule_snapshot | TEXT | 必填  | 判定依据规则快照 |
| source | VARCHAR(30) | 必填  | 数据来源 |
| event_id | VARCHAR(100) | 必填  | 客户端唯一事件ID |

表级完整性约束：FOREIGN KEY (elder_id) REFERENCES elder(id)；UNIQUE(source,event_id)

性能联合索引：ix_health_elder_time (elder_id,measured_at)；ix_health_time_abnormal (measured_at,abnormal)

## 9 家庭医生随访计划表（followup_plan）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| doctor_id | BIGINT | 必填  | 医生主键 |
| due_at | DATETIME(6) | 必填  | 计划应访时间 |
| status | VARCHAR(20) | 必填 默认 'PENDING' | 在册业务状态 |
| canceled_at | DATETIME(6) | 可空  | 计划取消时间 |
| cancel_reason | VARCHAR(500) | 可空  | 取消原因说明 |
| version | INT | 必填 默认 0 | 乐观锁并发版本号 |

表级完整性约束：FOREIGN KEY (elder_id) REFERENCES elder(id)；FOREIGN KEY (doctor_id) REFERENCES doctor(id)；CHECK (status IN ('PENDING','COMPLETED','CANCELED'))

性能联合索引：ix_plan_doctor_due (doctor_id,due_at)；ix_plan_elder_due (elder_id,due_at)

## 10 随访履约处置结果表（followup_record）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| plan_id | BIGINT | 必填 唯一  | 随访计划外键 |
| completed_at | DATETIME(6) | 必填  | 实际完成时间 |
| content | VARCHAR(2000) | 必填  | 随访过程记录 |
| result | VARCHAR(500) | 必填  | 处置结果摘要 |

表级完整性约束：FOREIGN KEY (plan_id) REFERENCES followup_plan(id)

性能联合索引：ix_followup_completed (completed_at)

## 11 智能穿戴腕表设备资产表（watch_device）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| serial_no | VARCHAR(80) | 必填 唯一  | 硬件出厂唯一序列号 |
| model | VARCHAR(80) | 必填  | 设备硬件型号 |
| enabled | BOOLEAN | 必填 默认 TRUE | 账号启用状态 |
| last_seen_at | DATETIME(6) | 可空  | 最近一次有效心跳 |
| version | INT | 必填 默认 0 | 乐观锁并发版本号 |

## 12 设备通信安全接入凭据表（device_credential）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| device_id | BIGINT | 必填 主键 自增 | 关联设备主键 |
| created_at | DATETIME(6) | 必填  | 创建生成时间 |
| rotated_at | DATETIME(6) | 可空  | 最近轮换时间 |

表级完整性约束：key_hash CHAR(64) NOT NULL UNIQUE；FOREIGN KEY (device_id) REFERENCES watch_device(id)

## 13 设备在线心跳上报事件表（device_heartbeat_event）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| device_id | BIGINT | 必填  | 关联设备主键 |
| event_id | VARCHAR(100) | 必填  | 客户端唯一事件ID |
| recorded_at | DATETIME(6) | 必填  | 定位采集时间 |
| received_at | DATETIME(6) | 必填  | 服务端接收时间 |

表级完整性约束：FOREIGN KEY (device_id) REFERENCES watch_device(id)；UNIQUE(device_id,event_id)

性能联合索引：ix_heartbeat_device_time (device_id,recorded_at,id)

## 14 设备老人配对绑定历史表（device_binding）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| device_id | BIGINT | 必填  | 关联设备主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| bound_at | DATETIME(6) | 必填  | 绑定建立时间 |
| unbound_at | DATETIME(6) | 可空  | 解除绑定时间 |
| active_device_id | BIGINT | 可空 唯一  | 当前有效绑定设备占位 |
| active_elder_id | BIGINT | 可空 唯一  | 当前有效绑定老人占位 |

表级完整性约束：FOREIGN KEY (device_id) REFERENCES watch_device(id)；FOREIGN KEY (elder_id) REFERENCES elder(id)；CHECK ((unbound_at IS NULL AND active_device_id IS NOT NULL AND active_elder_id IS NOT NULL AND active_device_id = device_id AND active_elder_id = elder_id) OR (unbound_at IS NOT NULL AND active_device_id IS NULL AND active_elder_id IS NULL))；CHECK (unbound_at IS NULL OR unbound_at >= bound_at)

性能联合索引：ix_binding_history (device_id,bound_at,unbound_at)

## 15 时序高频经纬度定位流水表（location_point）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| device_id | BIGINT | 必填  | 关联设备主键 |
| binding_id | BIGINT | 必填  | 发生时绑定外键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| event_id | VARCHAR(100) | 必填  | 客户端唯一事件ID |
| longitude | DECIMAL(10,7) | 必填  | WGS84标准经度 |
| latitude | DECIMAL(10,7) | 必填  | WGS84标准纬度 |
| recorded_at | DATETIME(6) | 必填  | 定位采集时间 |
| received_at | DATETIME(6) | 必填  | 服务端接收时间 |

表级完整性约束：FOREIGN KEY (device_id) REFERENCES watch_device(id)；FOREIGN KEY (binding_id) REFERENCES device_binding(id)；FOREIGN KEY (elder_id) REFERENCES elder(id)；UNIQUE(device_id,event_id)；CHECK (longitude BETWEEN -180 AND 180)；CHECK (latitude BETWEEN -90 AND 90)

性能联合索引：ix_location_elder_time (elder_id,recorded_at,id)；ix_location_device_time (device_id,recorded_at,id)

## 16 空间圆形电子围栏配置表（geofence）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| name | VARCHAR(80) | 必填  | 名称 |
| center_lon | DECIMAL(10,7) | 必填  | 围栏中心点经度 |
| center_lat | DECIMAL(10,7) | 必填  | 围栏中心点纬度 |
| radius_m | DOUBLE | 必填  | 防护半径(米) |
| enabled | BOOLEAN | 必填 默认 TRUE | 账号启用状态 |
| version | INT | 必填 默认 0 | 乐观锁并发版本号 |

表级完整性约束：CHECK (radius_m BETWEEN 50 AND 5000)；CHECK (center_lon BETWEEN -180 AND 180)；CHECK (center_lat BETWEEN -90 AND 90)

## 17 电子围栏监管纳管成员表（geofence_member）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| fence_id | BIGINT | 必填  | 所属围栏主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| enabled | BOOLEAN | 必填  | 账号启用状态 |

表级完整性约束：FOREIGN KEY (fence_id) REFERENCES geofence(id)；FOREIGN KEY (elder_id) REFERENCES elder(id)；UNIQUE(fence_id,elder_id)

## 18 围栏实时监测状态与快照表（geofence_state）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| member_id | BIGINT | 必填 唯一  | 围栏成员关联键 |
| last_point_id | BIGINT | 可空  | 最后参与判据的定位点 |
| last_recorded_at | DATETIME(6) | 可空  | 最新判定时间 |
| inside | BOOLEAN | 可空  | 是否在围栏内 |
| active_alert_id | BIGINT | 可空  | 关联活动告警事件 |

表级完整性约束：FOREIGN KEY (member_id) REFERENCES geofence_member(id)；FOREIGN KEY (last_point_id) REFERENCES location_point(id)

## 19 空间越界异常报警记录表（geofence_alert）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| member_id | BIGINT | 必填  | 围栏成员关联键 |
| exit_point_id | BIGINT | 必填  | 越界触发定位点 |
| triggered_at | DATETIME(6) | 必填  | 告警触发时间 |
| returned_at | DATETIME(6) | 可空  | 实际归巢返回时间 |
| closed_at | DATETIME(6) | 可空  | 告警闭环时间 |
| close_reason | VARCHAR(30) | 可空  | 闭环处置原因 |
| handled_by | BIGINT | 可空  | 接警处理人 |
| handled_at | DATETIME(6) | 可空  | 处理完成时间 |
| handling_note | VARCHAR(1000) | 可空  | 人工处置记录 |
| geometry_snapshot | TEXT | 必填  | 围栏几何参数快照 |
| version | INT | 必填 默认 0 | 乐观锁并发版本号 |

表级完整性约束：FOREIGN KEY (member_id) REFERENCES geofence_member(id)；FOREIGN KEY (exit_point_id) REFERENCES location_point(id)；FOREIGN KEY (handled_by) REFERENCES sys_user(id)

性能联合索引：ix_alert_member_time (member_id,triggered_at)；ix_alert_handled (handled_at)

## 20 关键业务操作安全审计日志表（audit_log）

| 字段名称 | 物理类型 | 约束与默认值 | 业务语义与说明 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| user_id | BIGINT | 可空  | 操作账号主键 |
| action | VARCHAR(60) | 必填  | 操作类型编码 |
| target_type | VARCHAR(60) | 必填  | 业务对象类型 |
| target_id | BIGINT | 可空  | 操作对象主键 |
| occurred_at | DATETIME(6) | 必填  | 操作记录时间 |
| result | VARCHAR(40) | 必填  | 执行结果码 |

表级完整性约束：FOREIGN KEY (user_id) REFERENCES sys_user(id)

性能联合索引：ix_audit_user_time (user_id,occurred_at)
