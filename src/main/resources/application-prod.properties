# ===================================================================
# 生产环境专用配置（spring.profiles.active=prod）
# ===================================================================

# 开启 Cookie Secure 属性，仅允许在 HTTPS 安全加密通道上传输会话凭据
server.servlet.session.cookie.secure=true

# 生产环境强制关闭演示种子数据注入，防止测试数据污染正式库
app.seed-demo=false

