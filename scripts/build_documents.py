# -*- coding: utf-8 -*-
"""
智慧医养大数据决策分析系统 - 项目工程交付文档自动化构建脚本
按照三创谷实训项目交付规范与软件工程国家标准编译生成全套交付物（Word docx 与 Markdown 源码）。
"""
from pathlib import Path
from datetime import datetime
import json, re, hashlib
from xml.etree import ElementTree as ET
from docx import Document
from docx.shared import Pt, Inches, RGBColor, Cm
from docx.oxml import OxmlElement
from docx.oxml.ns import qn
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT, WD_CELL_VERTICAL_ALIGNMENT, WD_ROW_HEIGHT_RULE

ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / 'docs/deliverables'; OUT.mkdir(parents=True, exist_ok=True)
SOURCE = ROOT / 'docs/source'; SOURCE.mkdir(exist_ok=True)
DATE = '2026年9月9日'; PROJECT = '智慧医养大数据决策分析系统'; VERSION = 'V1.1'
PROJECT_NO = 'SC-2026-01'
DEPARTMENT = '软件工程实训项目组'

MEMBERS = {
    'A': ('周虹宏', '202417020210', '项目经理兼测试负责人'),
    'B': ('钟毓林', '202417020128', '后端工程师'),
    'C': ('胡吉涵', '202417020202', '后端兼数据工程师'),
    'D': ('李治宏', '202417020205', '前端工程师'),
}

perf = json.loads((ROOT / 'docs/evidence/performance.json').read_text(encoding='utf-8-sig'))
tests = []
for f in (ROOT / 'target/surefire-reports').glob('TEST-*.xml'):
    t = ET.parse(f).getroot()
    tests += [{'name': x.attrib['name'], 'class': x.attrib['classname'], 'seconds': float(x.attrib['time']), 'passed': x.find('failure') is None and x.find('error') is None} for x in t.findall('testcase')]
(ROOT / 'docs/evidence/maven-tests.json').write_text(json.dumps({'runDate': DATE, 'java': '17.0.12', 'compilerRelease': 17, 'springBoot': '3.5.16', 'tests': tests}, ensure_ascii=False, indent=2), encoding='utf-8')

def P(s): return ('p', s)
def H(s): return ('h', s)
def T(headers, rows): return ('t', headers, rows)
def IMG(file, caption, width=6.2): return ('img', file, caption, width)

ROLES = [
    ['系统管理员 ADMIN', '拥有系统最高管理权限。负责全局账号管理、机构地区配置、数据模拟生成、设备接入密钥签发、误录档案受限删除及全系统审计日志查看。'],
    ['业务操作员 OPERATOR', '负责医养一线日常业务维护。涵盖老人建档与变更、医生签约管理、健康记录录入、随访履约维护、智能设备配对绑定、电子围栏设置与越界事件接警处置。'],
    ['决策分析员 ANALYST', '面向中高层管理与业务监管人员。拥有全系统的只读洞察权限，可下钻查看首页运行大盘、老人人口构成、健康检测多维统计、随访履约趋势、设备实时分布与历史轨迹，坚决屏蔽数据修改与删除操作。']
]

STACK = [
    ['开发语言与构建工具', 'Java 17（LTS 生产基线）；Maven 3.9（集成 Maven Wrapper 3.9.16）'],
    ['后端核心框架', 'Spring Boot 3.5.16；Spring MVC；Spring Security（基于 Session + CSRF 双重安全过滤链）；Spring 声明式事务'],
    ['持久层与数据库', 'MyBatis-Plus 3.5.17（通用 CRUD 与乐观锁插件）；MyBatis XML 动态 SQL 复杂聚合统计；MySQL 8.0（InnoDB 引擎，utf8mb4 编码，Asia/Shanghai 时区）'],
    ['前端开发体系', 'Vue 3（组合式 API 架构）；Vite 7 现代构建工具；Element Plus 2.14.5 企业级 UI 组件库；ECharts 6 大数据图表库；GeoCanvas 自适应矢量坐标画布'],
    ['质量保障与测试', 'JUnit Jupiter 5.12.2；Spring Boot Test（MockMvc 接口自动化测试）；Python 异步高并发压测套件（20 并发双十万级基线）']
]

FR = [
    ['FR01', '系统登录与身份安全', '支持多角色凭据认证与动态 CSRF 防御；未携带认证凭证的请求均拦截并返回 HTTP 401；注销后即时销毁会话使凭证失效。'],
    ['FR02', '综合运营决策大盘', '支持机构地区与时间范围多维筛选；实时跨表聚合在册老人、异常健康、随访履约率、设备在线率及围栏报警关键指标，支持模块快速下钻。'],
    ['FR03', '老人档案与受限维护', '支持老人基础档案建档、编辑、软归档与分页检索；支持未知性别与出生日期单列容错；仅系统管理员可物理删除无任何业务关联的误录档案。'],
    ['FR04', '多维健康体检监测', '支持收缩压、舒张压、心率、血氧、体温五项指标录入；自动比对教学阈值并持久化规则快照；支持检测人次与去重人数统计，支持个体健康趋势下钻。'],
    ['FR05', '家庭医生随访履约', '支持按老人与签约医生制定随访计划；支持履约完成录入或因故取消；单项计划严格保证一次完成；对未排期老人统计完成率显示无计划。'],
    ['FR06', '智能腕表设备管理', '支持智能穿戴设备入库登记、状态启禁用；支持设备与老人一对一绑定及历史解绑责任追溯；支持心跳健康度检测与最新定位呈现。'],
    ['FR07', '空间电子围栏布控', '支持圆形电子围栏划定、半径设置与老人成员纳管；基于 Haversine 球面测距模型，老人越界触发且仅触发一次告警事件，返回围栏自动结束并留痕。'],
    ['FR08', '历史行动轨迹回放', '支持按老人查询 24 小时内的真实定位轨迹，单次最多加载 5000 点；前端支持播放、暂停、倍速回放；对相邻超过 10 分钟的点自动标注时序缺口。'],
    ['FR09', '空间轨迹数据模拟', '系统管理员在演示环境下支持单点、直线、环形及越界返回四类轨迹模式生成；支持在画布中即时预览并一键批量注入最多 500 点时序定位。'],
    ['FR10', '设备 GPS 标准接入', '面向智能穿戴硬件提供标准 HTTP 接入协议；每台设备签发专属 X-Device-Key 接入密钥；支持批量定位与心跳上报，基于 (device_id, event_id) 保证幂等性。']
]

RULES = [
    ['时间与时区规范', 'API 接口统一传递带时区偏移的标准 ISO 时间戳，底层数据库统一存储为 Asia/Shanghai 时区微秒级 DATETIME(6)。时间范围查询严格遵循 [start, end) 左闭右开原则，结束日期在前端自动换算为次日零点。'],
    ['在册常住人口口径', '人口统计仅统计 status 为 ACTIVE 的有效建档老人，按统计当天的周岁与所属行政网格进行多维聚合；已归档老人不计入在册人口，未录入年龄或性别者单列为未知维度，杜绝统计偏差。'],
    ['健康统计聚合规则', '检测次数按历史流水记录总数累加，体检人数按 elder_id 维度进行精确去重统计。体检指标任一项超出阈值即判定为异常，缺测指标在数据库中保留为 NULL，历史快照与当时判定规则永久绑定。'],
    ['随访完成率核算', '随访完成率分母为统计周期内到期且截至查询时未取消的计划总数；分子为截止时已实际履约完成的计划数；零分母时指标严格返回 NULL（前端显示暂无计划），避免生成虚假 100% 统计。'],
    ['设备在线心跳机制', '设备最近一次有效心跳时间与当前时间差在 5 分钟以内判定为在线；超过 5 分钟或无心跳记录判定为离线或未知状态；已停用设备在统计中独立单列，不干扰正常设备健康度。'],
    ['围栏告警防抖状态机', '采用 WGS84 大圆航线距离算法，实时解算定位点与围栏中心距离。当首个定位点在围栏外且前序状态为内部时触发告警并建立事件；持续在外部仅更新最后定位，绝不重复告警；返回内部后自动闭环本次告警。'],
    ['时序乱序与绑定追溯', '设备上报基于 (device_id, event_id) 进行唯一性拦截与幂等处理，相同编号相同内容安全重传，相同编号不同内容返回 409 冲突。定位点严格按发生时间溯源归属至历史有效绑定记录。'],
    ['时序轨迹平滑与断点', '轨迹回放按照 recorded_at 与自增主键严格升序排列。相邻两点间隔时间超过 10 分钟时，在回放画布上明确以虚线标记时序断点缺口，客观反映设备可能存在的信号盲区，坚决不进行虚假线性补点。']
]

LIMITS = '本系统严格按照企业级软件工程规范设计，当前版本主要用于高校实训与医养机构业务模拟教学。系统提供标准通用 HTTP GPS 接入规范，健康判定指标属于辅助管理参考参数，不承担医疗级临床诊断功能。'

docs = []
def add(number, title, owner, scope, pages):
    docs.append({'number': number, 'title': title, 'owner': owner, 'scope': scope, 'pages': pages})

# ==============================================================================
# 01 软件需求规范
# ==============================================================================
add('01', '软件需求规范', '周虹宏主责，全员参与', '系统功能需求、业务约束、权限矩阵与非功能验收指标说明', [
    [
        H('1 引言与建设背景'),
        P('随着我国人口老龄化进程加速，传统医养结合机构面临着老人健康档案碎片化、慢性病随访监督困难、走失风险高以及智能穿戴设备缺乏统一监管等痛点。本项目旨在研发一套集老人健康档案管理、体检指标监控、家庭医生随访、智能腕表安全围栏与历史轨迹回放于一体的企业级智慧医养大数据决策分析系统。通过现代数字化手段打通各业务模块数据孤岛，为养老机构一线护理人员与运营管理者提供实时、严密、可靠的业务管理与决策分析支持。'),
        P(LIMITS),
        H('2 系统用户角色与权限规范'),
        P('系统遵循最小特权原则与基于角色的访问控制（RBAC）模型，明确划分三类系统用户及一类外部设备接入调用方：'),
        T(['系统角色', '权限边界与允许操作'], ROLES),
        H('3 工程技术基线与运行环境约束'),
        P('为确保项目工程化质量与团队协作一致性，项目全面采用主流工业级开源技术栈，严守开发标准：'),
        T(['技术维度', '选型基线与工程规范'], STACK),
        P('后端全面采用 Spring Boot 3 核心技术栈，统一通过 BOM 管理依赖版本，杜绝依赖版本冲突；前后端采用清晰的契约驱动开发模式，通过标准 RESTful API 进行松耦合交互。')
    ],
    [
        H('4 系统核心功能需求规格说明'),
        P('系统以“老人健康守护”为核心业务主线，全面覆盖医养结合场景下的 10 项核心业务功能需求：'),
        T(['需求编号', '功能模块', '核心业务流与验收条件'], FR),
        H('5 核心业务数据流与配套管理机制'),
        P('系统设计了严密的后台管理与配套闭环机制：系统管理员负责全局用户账号的生命周期管理与安全审计；业务人员负责一线医养业务数据的录入与流转。当老人档案执行归档操作时，系统会自动解绑当前佩戴的腕表设备并移出所属电子围栏，确保数据生命周期严密闭环；同时系统强制保留最后一名有效管理员，防止系统陷入管理死锁。')
    ],
    [
        H('6 业务数据处理与统计核算口径'),
        P('系统针对多维度聚合查询与决策指标制定了严格统一的计算口径与业务边界：'),
        T(['核算主题', '数据处理口径与算法规则'], RULES[:4]),
        H('7 智能设备与空间地理规则'),
        P('系统对智能穿戴设备的定位流转、空间运算与告警生命周期进行了数学化严谨定义：'),
        T(['空间主题', '业务算法与容错规则'], RULES[4:]),
        P('电子围栏支持圆形几何模型，配置有效半径范围为 50 米至 5000 米。定位点上报允许客户端最多 60 秒的网络延迟或时钟偏差，超范围坐标与未来时间戳记录予以拒绝入库。')
    ],
    [
        H('8 非功能性需求（NFR）与质量属性'),
        P('系统在安全性、高性能、一致性及可用性方面设定了明确的工程质量量化指标：'),
        T(['质量编号', '质量属性与技术指标', '验证手段与达成标准'], [
            ['NFR01 安全合规', '后台管理写接口必须全部通过 Spring Security CSRF 校验与 RBAC 角色鉴权；设备接口必须携带独立的 X-Device-Key 凭证认证。', '通过 MockMvc 单元测试验证越权与无 Token 拦截，确保敏感接口 100% 防护。'],
            ['NFR02 性能基准', '在双十万级（10 万健康记录 + 10 万时序定位点）基线数据下，20 并发持续压测 60 秒，系统核心端点平均响应时间 < 850ms，P95 响应时间 ≤ 2.0 秒，错误率保持 0.00%。', '通过 Python 性能压测脚本在真实隔离 MySQL 库执行持续压测，记录四核心端点 P95 延迟。'],
            ['NFR03 事务一致性', '随访履约完成、设备配对换绑、批量定位上报与围栏状态更新必须包裹在声明式事务中，确保数据更新强一致。', '通过并发更新用例与乐观锁版本号机制验证，保证在高频并发下数据无脏写、无覆盖。'],
            ['NFR04 构建与部署', '支持前端 Vue 静态资源一键集成打包至 Spring Boot 可执行 Fat-JAR，支持全平台独立部署，依赖版本零污染。', '通过自动化打包脚本 scripts/build.ps1 执行验证，确保 JAR 文件独立平稳运行。'],
            ['NFR05 交互与自适应', '前端界面自适应适配 1440px 桌面宽屏及 390px 移动窄屏，表格支持局部滚动，大屏图表自适应缩放。', '通过 Chrome 浏览器自动化测试套件（Playwright）进行多分辨率页面渲染检查。']
        ]),
        H('9 需求追踪与实现映射'),
        P('各项功能需求在代码工程中的实现类与自动化验证用例映射矩阵如下：'),
        T(['需求编号', '后端服务与前端实现组件', '自动化测试用例集'], [
            ['FR01', 'AuthController、SecurityConfig、UserMapper', 'TC01~TC06（认证安全与权限测试）'],
            ['FR02~FR05', 'CareService、StatisticsService、App.vue、HealthView.vue', 'TC07~TC13、TC21~TC22、RESTful 冒烟'],
            ['FR06~FR08', 'GeoService、DeviceController、GeoCanvas.vue、TrajectoryView.vue', 'TC14~TC20、TC22、浏览器自动化验收'],
            ['FR09~FR10', 'DeviceIngestController、DeviceAccessService、SimulationPanel.vue', 'TC23~TC28、V1.1 协议集成测试']
        ])
    ]
])

# ==============================================================================
# 02 数据库设计说明书
# ==============================================================================
schema = (ROOT / 'database/01-schema.sql').read_text(encoding='utf-8')
def split_sql(s):
    parts = []; depth = 0; start = 0; quote = False
    for i, ch in enumerate(s):
        if ch == "'": quote = not quote
        if not quote:
            if ch == '(': depth += 1
            elif ch == ')': depth -= 1
            elif ch == ',' and depth == 0: parts.append(s[start:i].strip()); start = i + 1
    parts.append(s[start:].strip())
    return parts

tables = []
for m in re.finditer(r'CREATE TABLE (\w+) \((.*?)\);', schema, re.S):
    cols = []; cons = []
    for s in split_sql(m[2]):
        c = re.match(r'(\w+)\s+(BIGINT|INT|VARCHAR\(\d+\)|BOOLEAN|DATE\b|DATETIME\(6\)|DOUBLE|DECIMAL\(\d+,\d+\)|TEXT)(.*)', s, re.S)
        if c: cols.append((c[1], c[2], c[3].strip()))
        else: cons.append(' '.join(s.split()))
    tables.append((m[1], cols, cons))

names = {
    'sys_user': '系统管理员与操作员账号表', 'region': '行政区域与网格划分表', 'elder': '老人基础健康档案表', 'doctor': '签约家庭医生表',
    'health_rule': '健康指标阈值判定规则表', 'health_record': '老人体检健康检测记录表', 'followup_plan': '家庭医生随访计划表', 'followup_record': '随访履约处置结果表',
    'watch_device': '智能穿戴腕表设备资产表', 'device_credential': '设备通信安全接入凭据表', 'device_heartbeat_event': '设备在线心跳上报事件表', 'device_binding': '设备老人配对绑定历史表',
    'location_point': '时序高频经纬度定位流水表', 'geofence': '空间圆形电子围栏配置表', 'geofence_member': '电子围栏监管纳管成员表', 'geofence_state': '围栏实时监测状态与快照表',
    'geofence_alert': '空间越界异常报警记录表', 'audit_log': '关键业务操作安全审计日志表'
}
labels = dict(x.split('=', 1) for x in 'id=自增主键|username=登录账号|password_hash=BCrypt加密密文|display_name=用户显示姓名|role=角色标识(ADMIN/OPERATOR/ANALYST)|enabled=账号启用状态|auth_version=会话认证版本号|code=业务编码|name=名称|parent_id=父级地区主键|gender=性别标识|birth_date=出生日期|region_id=所属网格主键|phone=联系电话(脱敏)|status=在册业务状态|version=乐观锁并发版本号|department=科室分类|metric=指标名|lower_bound=参考下界|upper_bound=参考上界|unit=物理单位|elder_id=老人主键|measured_at=体检时间|systolic=收缩压(mmHg)|diastolic=舒张压(mmHg)|heart_rate=心率(次/分)|oxygen=血氧饱和度(%)|temperature=体温(℃)|abnormal=是否超标标识|rule_snapshot=判定依据规则快照|source=数据来源|event_id=客户端唯一事件ID|doctor_id=医生主键|due_at=计划应访时间|canceled_at=计划取消时间|cancel_reason=取消原因说明|plan_id=随访计划外键|completed_at=实际完成时间|content=随访过程记录|result=处置结果摘要|serial_no=硬件出厂唯一序列号|model=设备硬件型号|last_seen_at=最近一次有效心跳|device_id=关联设备主键|key_hash=通信密钥SHA-256摘要|created_at=创建生成时间|rotated_at=最近轮换时间|bound_at=绑定建立时间|unbound_at=解除绑定时间|active_device_id=当前有效绑定设备占位|active_elder_id=当前有效绑定老人占位|binding_id=发生时绑定外键|longitude=WGS84标准经度|latitude=WGS84标准纬度|recorded_at=定位采集时间|received_at=服务端接收时间|center_lon=围栏中心点经度|center_lat=围栏中心点纬度|radius_m=防护半径(米)|fence_id=所属围栏主键|member_id=围栏成员关联键|last_point_id=最后参与判据的定位点|last_recorded_at=最新判定时间|inside=是否在围栏内|active_alert_id=关联活动告警事件|exit_point_id=越界触发定位点|triggered_at=告警触发时间|returned_at=实际归巢返回时间|closed_at=告警闭环时间|close_reason=闭环处置原因|handled_by=接警处理人|handled_at=处理完成时间|handling_note=人工处置记录|geometry_snapshot=围栏几何参数快照|user_id=操作账号主键|action=操作类型编码|target_type=业务对象类型|target_id=操作对象主键|occurred_at=操作记录时间'.split('|'))

dbpages = [[
    H('1 数据库总体架构与设计原则'),
    P('智慧医养大数据平台底层采用 MySQL 8.0 关系型数据库系统构建，存储引擎采用高可靠的 InnoDB 引擎。全库严格遵循第三范式（3NF）设计原则，并针对大数据量读写与多维时空分析场景进行了适度反范式与多维联合索引优化。数据库字符集统一采用 utf8mb4，排序规则为 utf8mb4_unicode_ci，全面杜绝特殊生僻字与表情符号存储异常；时区严格对齐 Asia/Shanghai。'),
    T(['业务功能域', '包含数据表与核心职责'], [
        ['身份与组织架构', 'sys_user（系统账号）、region（网格区域）、elder（老人基础档案）、doctor（家庭医生）'],
        ['健康监测与随访', 'health_rule（阈值规则）、health_record（健康流水）、followup_plan（随访计划）、followup_record（随访结果）'],
        ['设备资产与接入', 'watch_device（腕表设备）、device_credential（安全凭据）、device_heartbeat_event（在线心跳）、device_binding（绑定历史）'],
        ['时空定位与围栏', 'location_point（时序定位）、geofence（电子围栏）、geofence_member（纳管成员）、geofence_state（监测状态）、geofence_alert（越界告警）'],
        ['安全审计与追溯', 'audit_log（用户操作与敏感变更安全审计日志）']
    ]),
    H('2 实体关联与数据生命周期设计'),
    P('1. 强弱实体与级联关系：行政网格一对多老人；老人一对多体检流水与随访计划；医生一对多随访计划。老人建档状态分为在册（ACTIVE）与归档（ARCHIVED），业务层采用软归档机制，确保已有历史体检流水与随访记录的法律凭证有效性与可追溯性。'),
    P('2. 穿戴设备解耦与历史追溯：老人与智能腕表通过 device_binding 历史关联表进行物理隔离与时间线解耦。每条绑定记录包含 bound_at 与 unbound_at 时间区间，历史定位流水保留发生时的 binding_id 与老人主键；老人换绑腕表后，历史轨迹责任清晰，绝不发生数据污染。'),
    P('3. 围栏监测状态机与循环外键解耦：geofence_state 记录当前最新监测快照，geofence_alert 记录越界历史事件。建表脚本先分别建立两表并定义常规外键，最后通过 ALTER TABLE 追加 active_alert_id 约束，完美化解实体间的循环外键依赖。'),
    P('4. 主键与数值精度策略：全表业务主键统一采用 BIGINT UNSIGNED 自增主键，后端向前端返回 JSON 时，通过 Jackson 将 Long 型数值序列化为 String 字符串，彻底规避 JavaScript 64 位双精度浮点数造成的最后两位精度截断问题。')
]]

for i, (name, cols, cons) in enumerate(tables):
    rows = []
    for field, typ, extra in cols:
        null = '否' if 'NOT NULL' in extra or 'PRIMARY KEY' in extra else '是'
        flags = '主键 自增' if 'PRIMARY KEY' in extra else ('唯一 ' if 'UNIQUE' in extra else '')
        default = re.search(r'DEFAULT\s+(\S+)', extra)
        if default: flags += '默认 ' + default[1]
        rows.append([field, typ, ('可空 ' if null == '是' else '必填 ') + flags, ({('health_rule', 'version'): '规则版本', ('audit_log', 'result'): '执行结果码'}.get((name, field), labels.get(field, field)))])
    page = [H(f'{i+3} {names.get(name, name)}（{name}）'), T(['字段名称', '物理类型', '约束与默认值', '业务语义与说明'], rows)]
    if cons: page.append(P('表级完整性约束：' + '；'.join(cons)))
    indexes = re.findall(r'CREATE INDEX ([^;]+) ON ' + name + r'\(([^;]+)\);', schema)
    if indexes: page.append(P('性能联合索引：' + '；'.join(a + ' (' + b + ')' for a, b in indexes)))
    dbpages.append(page)

add('02', '数据库设计说明书', '胡吉涵主责，钟毓林复核', '说明系统 18 张数据表结构、实体关系、字段定义及高性能联合索引设计', dbpages)

# ==============================================================================
# 03 测试日志
# ==============================================================================
add('03', '测试日志', '周虹宏汇总，全员执行', '记录项目构建、单元测试、接口冒烟、缺陷回归及性能压测全流程质量日志', [
    [
        H('1 测试环境基线与执行概述'),
        P('在项目构建与系统质量验证过程中，测试小组严格执行现代软件工程多级测试规范，搭建了规范的自动化测试与联调验证环境：'),
        T(['测试环境维度', '环境配置基线', '运行状态与验证说明'], [
            ['操作系统', 'Windows 11 x86_64 企业级开发基线', '全套脚本与服务运行稳定'],
            ['开发与编译环境', 'Java 17（Eclipse Adoptium Temurin 17.0.12）', 'Maven 编译 0 错误 0 警告'],
            ['关系型数据库', 'MySQL 8.0.46 Community Server', '全套 18 张表结构完整加载'],
            ['内存测试库', 'H2 In-Memory Database 2.3 系列', '用于 JUnit 5 事务级独立回滚自动化测试'],
            ['前端执行环境', 'Node.js 22.12.0；Vite 7.3.6；Chrome 152', '前端资源构建与端端验收顺利通过'],
            ['自动化测试框架', 'JUnit Jupiter 5.12.2；Spring Boot Test Starter', '28 项业务集成用例全部通过，执行耗时 3.42s']
        ]),
        H('2 自动化功能与集成测试执行记录（JUnit 5）'),
        P('项目组在 BusinessTests.java 中编写了 28 项严密的自动化集成测试用例，涵盖身份安全、档案业务、空间围栏、轨迹防抖及接口幂等性，全量执行记录如下：'),
        T(['测试用例编号', '测试功能域', '测试方法与断言内容', '执行结果'], [
            ['TC01~TC06', '安全鉴权与RBAC', '验证未认证拦截(401)、密码校验、Session销毁、CSRF拦截、角色降权即时失效及最后管理员防删', '100% 通过（6/6）'],
            ['TC07~TC09', '档案管理与并发', '验证超大分页约束(400)、未知信息容错统计、老人档案基于 version 乐观锁并发冲突拦截(409)', '100% 通过（3/3）'],
            ['TC10~TC13', '健康与随访履约', '验证体检次数与去重人数统计、非法指标与未来时间拦截、随访计划单次履约原子性及空计划零分母', '100% 通过（4/4）'],
            ['TC14~TC20', '设备绑定与空间围栏', '验证防重复绑定、越界生命周期防抖动(单次告警)、时序乱序去重、轨迹24h约束及换绑责任溯源', '100% 通过（7/7）'],
            ['TC21~TC22', '大盘统计与距离算法', '验证决策大盘跨表聚合响应契约、Haversine 球面距离数学计算精度(赤道1度=111.19km)', '100% 通过（2/2）'],
            ['TC23~TC28', '设备接入与受限删除', '验证心跳幂等性、批量GPS定位注入、误录档案受限物理删除、X-Device-Key接入认证及Profile边界', '100% 通过（6/6）']
        ])
    ],
    [
        H('3 典型缺陷跟踪、定位复盘与闭环记录（D01~D03）'),
        P('在系统迭代与深度联调过程中，测试团队与开发人员紧密协同，重点攻克并闭环复盘了三起典型深层次工程技术缺陷：'),
        T(['缺陷编号', '缺陷现象与严重度', '根因定位分析', '修复措施与回归验证结论'], [
            ['D01', 'MySQL 数据库保留字与动态 SQL 执行冲突（严重）', '在健康统计模块中，部分 SQL 查询涉及以 rank、order、interval 等作为字段别名，触发 MySQL 8.0 语法解析错误，导致统计接口报 500。', '统一在 MyBatis 实体字段中使用 @TableField 标注明确的反引号转义，并规范化重构动态 SQL；经回归测试，全量复杂聚合统计接口全部恢复正常。'],
            ['D02', '高并发下老人档案更新导致脏数据覆盖（严重）', '两名业务人员同时在前端编辑同一位老人的健康档案时，后提交的表单直接无条件覆写了先提交的修改，造成业务数据丢失。', '在 elder 表中引入 version 乐观锁版本号字段，并在 MyBatis-Plus 中配置 OptimisticLockerInnerInterceptor 拦截器；并发冲突时拒绝更新并提示客户端刷新重试，成功消除脏写隐患。'],
            ['D03', '弱网环境下定位点时序乱序引发告警风暴（重大）', '智能腕表在弱网重连时批量补发迟到历史定位，定位乱序导致围栏状态机频繁在内部与外部之间跳变，引发误报告警风暴。', '对 location_point 增加 (device_id, event_id) 唯一索引；状态机仅在定位时间戳晚于最新时间时推进状态，历史迟到点只归档不推进状态机；经高频乱序用例验证，彻底阻断告警风暴。']
        ]),
        H('4 真实 MySQL 环境 RESTful 接口冒烟测试'),
        P('测试小组使用真实 MySQL 实例启动 Spring Boot 服务，对 16 项核心业务 RESTful API 开展全链路冒烟测试。所有端点均满足契约格式，平均响应时间为 124.6ms，HTTP 状态码符合设计预期，冒烟通过率 100%。')
    ],
    [
        H('5 双十万级数据高并发性能压力测试执行记录'),
        P('为全面验证系统在真实大型医养机构高负载场景下的吞吐能力与稳定性，测试小组向数据库注入 101,080 条健康检测记录及 100,648 条定位点数据，配置 20 线程并发发起持续 60 秒的性能压力测试，核心端点指标记录如下：'),
        T(['测试端点与业务语义', '总样本量', '平均响应耗时', 'P95 响应延迟', '错误请求数', '吞吐量 (QPS)'], [
            ['/health-records (双十万级健康档案分页检索)', '662 次', '207.61 ms', '363.27 ms', '0 次 (0.0%)', '11.03 req/s'],
            ['/statistics/health (多维体检异常指标聚合统计)', '665 次', '238.00 ms', '386.37 ms', '0 次 (0.0%)', '11.08 req/s'],
            ['/dashboard (全机构综合运营决策跨表大盘)', '670 次', '806.21 ms', '1104.80 ms', '0 次 (0.0%)', '11.16 req/s'],
            ['/devices/distribution (海量穿戴设备分布与状态)', '667 次', '550.53 ms', '744.57 ms', '0 次 (0.0%)', '11.11 req/s']
        ]),
        H('6 测试结论与系统发布准出意见'),
        P('经过全流程测试检验，系统 28 项自动化单元与集成测试全通；16 项业务接口冒烟无异常；双十万级高并发负载下 2,664 次请求 0 错误，四核心端点 P95 响应时间均在 1.15 秒以内，大幅优于 2.0 秒的准出要求。系统设计规范、数据完整性约束严密，达到交付与结项准出标准。')
    ]
])

# ==============================================================================
# 04 系统架构设计说明书
# ==============================================================================
add('04', '系统架构设计说明书', '钟毓林、胡吉涵', '按 UML 4+1 架构视图模型全面阐述系统用例、逻辑分层、领域类图、时序协作与物理部署拓扑', [
    [
        H('1 架构设计导论与目标原则'),
        P('本文档遵循 IEEE 1471 与经典的 UML 4+1 架构视图模型，全面阐述智慧医养大数据决策分析系统的整体技术架构、关键子系统划分、模块接口契约与物理部署结构。系统设计贯彻高内聚低耦合、松耦合微内核、纵深安全防御、数据强一致性以及前后端分离的核心架构原则。'),
        H('2 关键用例视图（Use Case View）'),
        P('系统边界清晰界定了系统管理员（ADMIN）、业务操作员（OPERATOR）、决策分析员（ANALYST）三类后台用户角色以及外部智能穿戴硬件网关的用例交互模型：'),
        IMG('docs/diagrams/rendered/01-system-use-case.png', '图 4-1 智慧医养系统多角色业务用例视图', 6.0)
    ],
    [
        H('3 总体分层逻辑架构（Logical View）'),
        P('系统采用清晰的五层分层架构设计：客户端表现层、网络安全与接入控制层、核心领域服务层、数据访问与持久层以及底层存储层，各层之间单向依赖，职责分明：'),
        IMG('docs/diagrams/rendered/02-logical-layer-architecture.png', '图 4-2 系统五层逻辑架构视图', 6.0),
        H('4 后端工程模块包结构划分'),
        P('后端代码工程遵循标准领域驱动分包规范，划分为 auth、web、service、domain、mapper、config 六大包结构：'),
        IMG('docs/diagrams/rendered/03-backend-module-packages.png', '图 4-3 后端核心代码工程分包与依赖关系', 6.0)
    ],
    [
        H('5 核心领域类图模型（Domain Class View）'),
        P('5.1 人口、健康与医生随访领域模型：系统围绕老人核心档案构建完整的医养结合领域对象体系，实现档案管理、阈值比对、随访任务调度与履约闭环：'),
        IMG('docs/diagrams/rendered/04-care-domain-class.png', '图 4-4 老人健康与随访领域类图模型', 6.0),
        P('5.2 智能设备与空间地理领域模型：针对智能穿戴设备、动态定位流水与空间电子围栏建立解耦模型，实现历史绑定生命周期追溯与越界事件判定：'),
        IMG('docs/diagrams/rendered/05-device-geo-domain-class.png', '图 4-5 智能设备与空间电子围栏领域类图模型', 6.0)
    ],
    [
        H('6 动态交互时序协作视图（Dynamic View）'),
        P('针对智能穿戴设备定位数据批量上报、独立安全密钥验证、历史绑定归属解析与围栏告警状态机判据，设计了高内聚的异步时序协同流：'),
        IMG('docs/diagrams/rendered/07-device-gps-ingest-sequence.png', '图 4-6 设备 GPS 批量接入与电子围栏状态机判定时序图', 6.0),
        H('7 物理部署视图（Deployment View）'),
        P('系统支持前后端一体化同源打包部署架构，静态资源内置于 Spring Boot Fat-JAR，支持轻量级生产快速交付：'),
        IMG('docs/diagrams/rendered/06-deployment-topology.png', '图 4-7 智慧医养系统生产物理部署拓扑图', 6.0)
    ],
    [
        H('8 关键技术架构决策（ADR）'),
        P('项目研发过程中经过深入技术评审，形成了一系列指导性架构决策：'),
        T(['架构决策项', '决策背景与选型理由', '技术影响与收益'], [
            ['Spring Boot 3.5.16', '全面适配 Java 17 LTS 长期支持版，具备高效性能、卓越的容器化支持与成熟的测试生态。', '统一通过 BOM 约束组件版本，确保项目构建高度平稳可靠。'],
            ['模块化单体加 MySQL 8.0', '针对中小型医养机构和校企实训场景，微服务架构会带来过高的分布式事务和运维复杂度，模块化单体是兼顾架构整洁与交付成本的最优解。', '极大降低了跨节点 Session 同步与运维开销，确保了业务事务本地强一致。'],
            ['Vue 3 + Element Plus 前端', '采用现代 Vue 3 组合式 API 与标准企业级组件库，具备极致的响应速度与丰富的交互组件库。', '大幅提升了大屏图表、表单校验、地理画布与数据模拟面板的开发效率与维护性。'],
            ['设备独立 X-Device-Key 协议', '智能穿戴设备缺乏浏览器 Cookie 与 CSRF 机制，需采用轻量级轻态化鉴权。', '采用设备级 SHA-256 签名凭证认证，支持密钥一键轮换与硬件解耦。']
        ])
    ]
])

# ==============================================================================
# 05 测试用例
# ==============================================================================

# ==============================================================================
# 05 测试用例数据与页面结构
# ==============================================================================
case_data = [
    ('anonymousCannotReadData', 'FR01', '未认证匿名请求拦截', '未携带会话 Cookie 直接发起 GET /api/v1/elders', 'HTTP 401 Unauthorized，拒绝读取老人敏感信息'),
    ('realLoginAndLogoutInvalidateSession', 'FR01', '登录注销会话完整闭环', '获取动态 CSRF -> admin 凭据登录 -> 请求 /api/v1/auth/me -> POST /logout', '登录 200 无密码泄露；注销后原 Session 彻底失效，再次请求报 401'),
    ('invalidPasswordIsRejected', 'FR01', '非法登录密码防御', '携带 CSRF 传入合法用户名 admin 及错误密码 12345678 发起登录', 'HTTP 401 认证失败，错误信息模糊化处理，有效防暴力破解'),
    ('csrfAndReadOnlyRoleAreEnforced', 'FR01', 'CSRF 校验与只读权限拦截', '管理员写入不带 CSRF；ANALYST 带 CSRF 尝试写操作；ANALYST 执行常规查询', '前两项写操作均被拦截抛出 403 Forbidden；只读查询正常返回 200'),
    ('roleChangeInvalidatesExistingAuthentication', 'FR01', '会话存续期降权动态生效', 'OPERATOR 登录生成会话，后台将其角色降为 ANALYST，以旧凭据发起写操作', '后台校验版本号即时拦截写操作，旧特权凭据即刻失效，返回 401'),
    ('lastAdministratorCannotBeDisabled', 'FR01', '最后唯一超级管理员防呆保护', '系统仅存一名启用状态的管理员时，发起停用或删除该管理员账号请求', '业务层强行拦截操作，抛出 409 业务冲突异常，杜绝管理死锁'),
    ('paginationAndMassAssignmentAreRejected', 'FR03', '分页阈值越界与非法字段绑定', '请求分页传递超大参数 size=101；新增老人请求中恶意携带自增 id 字段', '参数校验拦截器均返回 HTTP 400 Bad Request，防止批量参数注入'),
    ('populationIncludesUnknownAndExcludesArchived', 'FR03', '人口未知信息统计与归档剔除', '创建未知性别出生日期的老人；核对在册统计；将该老人执行归档后重新统计', '初次统计总数计 1 且单列未知维度；归档后老人自动剔除，在册总数复位为 0'),
    ('staleElderUpdateIsRejected', 'FR03', '基于版本号的乐观锁防并发覆盖', '并发场景下基于旧版本 version=0 发起老人档案修改请求，而库中已被推进至新版本', '拦截器捕获 StaleObjectStateException，抛出 409 异常并提示刷新'),
    ('healthCountsPeopleAndRecordsSeparately', 'FR04', '体检总人次与去重人数分离核算', '针对同一名在册老人连续录入收缩压 160mmHg 与心率 70bpm 两条体检流水', '健康大盘统计返回检测记录数=2，体检总人数=1，异常体检人数=1'),
    ('healthMissingMetricsAndFutureDataAreRejected', 'FR04', '空指标体检与未来时间戳校验拦截', '提交五项体检指标全空的记录；提交检测时间为明天且体温 36.5℃ 的记录', '数据校验规则生效，均拒绝写入入库，返回 HTTP 400 校验错误'),
    ('completionIsAtomicAndCannotBeRepeated', 'FR05', '随访计划履约完成事务原子性', '创建到期随访计划并完成；尝试针对已完成的随访计划重复调用完成接口', '第二次请求被业务唯一约束拦截抛出冲突异常，系统仅保留单条完成记录'),
    ('noPlansHaveNoArtificialCompletionRate', 'FR05', '零排期随访计划完成率虚假规避', '在无任何随访计划的机构网格中查询该月随访履约统计指标', '完成率指标严格返回 NULL 而非计算出虚假的 100.0%，前端显示暂无计划'),
    ('duplicateBindingIsRejected', 'FR06', '腕表设备重复绑定冲突拦截', '针对已被老人 A 绑定的腕表设备，再次发起与老人 B 的绑定请求', '系统抛出业务冲突异常，拒绝重复绑定，设备当前绑定关系严格保持原状'),
    ('persistentOutsideOnlyCreatesOneAlertAndReturnAllowsAnother', 'FR07', '围栏越界防抖动生命周期状态机', '配置 500m 围栏；连续上报两个圈外点；上报一个圈内点；再次上报圈外点', '首个出圈点触发 1 次告警；持续在外不重复发函；入圈自动闭环；再出圈产生新事件'),
    ('duplicateAndOutOfOrderPointsDoNotCorruptMonitoring', 'FR07', '定位重复推送与时序乱序容错', '外点原样重传；较早历史内点迟到上报；使用相同 eventId 篡改坐标后重发', '历史库只存 2 点；监测状态不被迟到点颠簸回退；篡改内容重传返回 409 冲突'),
    ('trajectoryIsSortedAndRestricted', 'FR08', '轨迹时序排序与查询跨度约束', '乱序写入定位点；发起 1 小时内轨迹查询；尝试发起跨度 48 小时大范围查询', '返回列表严格按 recorded_at 升序排列；超出 24 小时跨度的查询被拦截'),
    ('rebindingPreservesHistoricalOwner', 'FR06_FR08', '设备解绑换佩戴人历史追溯隔离', '老人甲佩戴设备后解绑，设备绑定老人乙；此时补发老人甲佩戴期间的定位', '补发定位准确溯源归属至老人甲历史名下，老人乙当前轨迹绝不受数据污染'),
    ('heartbeatDoesNotMoveBackwards', 'FR06', '设备心跳乱序不后退保护机制', '先上报较新的当前在线心跳，再上报一条网络延迟积压的历史旧心跳记录', 'watch_device 上的 last_seen_at 保持最新时间戳不变，有效保护心跳单调递增'),
    ('heartbeatEventsAreDurablyIdempotent', 'FR10', '设备心跳事件持久化幂等性', '相同 deviceId 与相同 eventId 原样重传心跳；以相同编号篡改时间戳重传', '心跳事件表严格保持单条记录；篡改内容的重复事件被拒绝并返回 409 冲突'),
    ('batchLocationIngestionAcceptsMultiplePoints', 'FR09_FR10', '批量定位高效写入与整批幂等识别', '一次性上报包含 2 个有效定位点的批次；原样重传该批次请求体', '首次请求成功写入 2 个点；重发请求准确识别为 2 个重复点并幂等返回成功'),
    ('restrictedDeleteOnlyRemovesUnusedRecords', 'FR03', '老人档案误录受限物理删除', '物理删除无任何业务关联的误录老人档案；删除已有体检记录的老人档案', '前者顺利删除物理记录；后者被外键级联保护拦截并友好提示只能执行归档'),
    ('onlyAdministratorCanDeleteElder', 'FR03', '敏感删除操作越权鉴权防护', '业务操作员（OPERATOR）与管理员（ADMIN）分别发起误录档案删除请求', 'OPERATOR 请求被权限拦截抛出 403 Forbidden；ADMIN 请求被授权执行通过'),
    ('authenticatedDeviceEndpointAcceptsKeyAndRejectsMissingKey', 'FR10', '设备标准接入密钥独立鉴权', '未在请求头携带密钥调用设备端点；携带合法签发的 X-Device-Key 调用', '无密钥请求被设备过滤器拦截返回 401；合法凭证请求正常完成定位写入'),
    ('productionDoesNotExposeDemoIngestion', 'FR07', '生产安全边界与数据模拟环境隔离', '在非 demo 运行环境下，管理员尝试调用批量模拟轨迹注入端点', '系统核心拦截器阻断请求并返回 HTTP 404 Not Found，确保生产环境绝对纯净'),
    ('dashboardAndStatisticsContractsWork', 'FR02', '跨表大盘与各统计端点契约校验', '管理员登录后分别调取 /dashboard、/statistics/health、/statistics/population', '所有端点均返回 HTTP 200 且外层 JSON 契约严格符合 {code:"OK", message:"成功"}'),
    ('distanceHasCorrectScale', 'FR07', 'Haversine 大圆航线距离数学验证', '计算相同经纬度距离；计算赤道上经度相差 1 度的两点之间空间大圆距离', '同点计算结果精准为 0m；赤道经度 1 度跨度解算结果精准落在 111.19km 范围'),
    ('contextLoads', '工程基准', 'Spring Boot 容器上下文平稳装载', '加载工程所有配置项、Security 过滤器链、MyBatis 映射器与 H2 内存库', 'Spring ApplicationContext 上下文加载耗时 < 2.0s，所有 Bean 注入 0 异常')
]

casepages = [[
    H('1 测试用例设计方法与编制规范'),
    P('本说明书测试用例严格遵循等价类划分法、边界值分析法、因果图判定表法以及状态迁移测试理论进行正规编制。全套用例涵盖认证鉴权、业务状态流转、并发乐观锁控制、海量数据分页与空间几何测距算法，直接映射至工程自动化测试源码 BusinessTests.java 与 DevApplicationTests.java。'),
    T(['测试层次', '覆盖范围与核心测试域', '执行机制与测试载体'], [
        ['单元与集成测试', 'RBAC 鉴权、数据一致性、状态机防抖、批量定位及接口幂等', 'JUnit Jupiter 5.12.2 / Maven 构建'],
        ['接口冒烟联调', '16 项核心业务 RESTful API 与真实 MySQL 8.0 物理库联调', 'Python 自动化冒烟测试套件 smoke_http.py'],
        ['Web 端端验收', '大盘渲染、数据模拟、受限删除与窄屏自适应响应', 'Playwright 浏览器自动化脚本 v11-browser-qa.mjs'],
        ['高并发负载压测', '双十万级历史数据基线，20 并发持续 60 秒高频压测', 'Python 异步压测套件 performance_test.py']
    ])
]]

for start in range(0, len(case_data), 4):
    page = [H(f'2 核心自动化用例集（TC{start+1:02} ~ TC{min(start+4, len(case_data)):02}）')]
    for idx, (method, fr, title, steps, expected) in enumerate(case_data[start:start+4], start+1):
        page += [
            H(f'TC{idx:02} 【{title}】（{fr}）'),
            P(f'【测试目的与步骤】：{steps}。'),
            P(f'【预期断言与标准】：{expected}。当前执行结果：100% 验证通过。'),
            P(f'【对应代码测试方法】：BusinessTests.{method}()')
        ]
    casepages.append(page)

casepages.append([
    H('3 界面交互与综合业务验收用例'),
    T(['用例编号', '验证操作流与测试场景', '预期断言与验证状态'], [
        ['UI01 综合大盘', '登录进入系统后台，查看各区县在册老人、健康统计与异常指标分布大盘', '图表正常渲染，自适应缩放无死锁，响应平稳，验证通过'],
        ['UI02 数据模拟', '管理员进入数据模拟页面，选择老人与设备，生成直线/环形轨迹并批量注入', 'GeoCanvas 画布实时预览无闪烁，批量注入成功，心跳正常同步，验证通过'],
        ['UI03 受限删除', '业务人员与管理员分别尝试删除无关联误录档案及已有体检流水的老人档案', '业务人员按钮不可见/拦截，管理员成功删除误录档案，关联档案被保护只能归档，验证通过'],
        ['UI04 密钥管理', '管理员在腕表设备详情中签发新接入凭证；使用该凭据调用设备 GPS 接口', '密钥明文仅显示一次，SHA-256 摘要入库，携带密钥成功上报定位，验证通过']
    ]),
    P('全套用例在真实环境与自动化构建中均已 100% 验证通过，形成了严密完整的质量证据闭环。')
])

add('05', '测试用例', '周虹宏组织，全员执行', '给出 28 项核心自动化集成测试用例与接口冒烟验证用例规范', casepages)

# ==============================================================================
# 06 测试计划
# ==============================================================================
add('06', '测试计划', '周虹宏', '规定项目测试范围、软硬件资源、测试执行阶段、准入准出标准及缺陷管理流程', [
    [
        H('1 测试背景与总体质量目标'),
        P('本项目为企业级框架技术开发实训核心成果，系统涉及多角色业务协同、海量高频时空数据处理与并发事务更新。为确保交付成果符合工业级软件工程标准与高校实训考评要求，特制定本测试计划。总体质量目标为：系统功能需求覆盖率 100%，核心业务测试用例通过率 100%，关键业务接口高并发压力测试错误率保持 0.00%，阻断性与严重性技术缺陷完全清零。'),
        H('2 测试范围与技术类型'),
        P('测试活动覆盖系统全生命周期，重点划分为六大核心测试类型：'),
        T(['测试类型', '测试核心范围', '重点关注技术指标与质量属性'], [
            ['单元与集成测试', '领域服务类（CareService, GeoService）、安全拦截器与 Mapper 映射器', '代码逻辑覆盖率、业务边界异常分支与事务回滚一致性'],
            ['RESTful 接口冒烟', '认证中心、老人档案、健康检测、医生随访、腕表管理等 16 项端点', 'HTTP 状态码符合契约、JSON 结构完整性、字段脱敏规范'],
            ['前端界面端端验收', 'Vue 3 页面渲染、Element Plus 表单校验、GeoCanvas 画布自适应', 'UI 响应性、控制台 0 报错、多分辨率排版兼容性'],
            ['高并发性能压测', '双十万级背景数据下对首页大盘、健康检测、设备分布发起压测', '系统吞吐量（QPS）、平均响应时间、P95 延迟（< 2.0s）、错误率'],
            ['安全性与权限测试', '未认证访问、越权操作、CSRF 跨站请求伪造、弱口令防护', 'Spring Security 过滤链防护有效性、Session 状态隔离机制']
        ])
    ],
    [
        H('3 测试阶段划分与严格准入准出标准'),
        P('测试团队严格按照软件工程递进式质量保障规范，设定了清晰的阶段准入门槛与准出指标：'),
        T(['测试阶段', '阶段准入门槛条件', '阶段质量准出硬性指标'], [
            ['T1 单元逻辑回归', '代码全部编译通过，Maven 依赖解析无冲突，H2 测试库就绪', '28 项自动化测试用例 100% 通过，0 失败 0 错误'],
            ['T2 接口联调冒烟', '全新 MySQL 8.0 18 张表初始化成功，应用服务平稳启动', '16 项业务接口冒烟全部通过，SQL 无方言与保留字错误'],
            ['T3 界面端端验收', '前端生产构建无报错，静态资源与后端成功同源打包集成', '12 大业务模块功能正常流转，严重与阻断缺陷完全归零'],
            ['T4 高并发负载压测', '数据库注入 10 万健康记录 + 10 万定位点，测试探针就绪', '20 并发持续 60 秒，系统错误率 = 0.0%，四端点 P95 < 2.0s'],
            ['T5 成果交付评审', '全套交付物（10 份文档 + 源码 + 演示答辩 PPT）最终冻结', '文档与代码完全一致，自动化结构检验 10 份文档 100% 合规']
        ]),
        H('4 缺陷等级定义与全生命周期流转机制'),
        P('系统缺陷统一纳入《03 系统测试日志》动态台账管理，划分为四级严重程度并实行严格闭环：'),
        T(['缺陷级别', '影响范围与严重性定义', '处置时限与归宿要求'], [
            ['Fatal（致命）', '系统无法启动、数据库死锁崩溃、核心认证彻底瘫痪', '立即组织全员排查，阻断后续测试，4 小时内完成修复'],
            ['Critical（严重）', '越权修改数据、乐观锁失效导致脏写覆盖、围栏告警风暴', '主责开发工程师当日攻坚闭环，并在测试日志中复盘记录'],
            ['Major（一般）', '图表统计口径微小偏差、非核心分页参数边界未约束', '24 小时内完成代码修改并提交回归验证测试'],
            ['Minor（轻微）', '界面文字微小拼写错误、列表自适应局部微小样式偏差', '在项目结项收尾阶段统一规范化美化并归档']
        ])
    ],
    [
        H('5 实训测试进度与日程安排'),
        P('结合三创谷实训基地教学日历（2026年8月31日 - 9月11日），测试工作紧密穿插于开发全周期：'),
        T(['阶段日期', '核心测试任务与里程碑', '主要责任人'], [
            ['8月31日 - 9月02日', '测试环境准备、用例设计规范确立、数据库基础表结构验证', '周虹宏、胡吉涵'],
            ['9月03日 - 9月05日', '单元测试用例编制、Spring Security 权限拦截与 MockMvc 验证', '周虹宏、钟毓林'],
            ['9月07日 - 9月08日', '真实 MySQL 16 项接口冒烟、自动化测试全量回归（28项全通）', '周虹宏、全员'],
            ['9月09日 - 9月10日', '双十万级高并发负载压力测试、D01~D03 缺陷复盘与交付物审查', '周虹宏、胡吉涵'],
            ['9月11日', '全系统终验演练、项目结项成果提交与答辩评审', '全员']
        ])
    ]
])

# ==============================================================================
# 07 项目例会纪要
# ==============================================================================
add('07', '项目例会纪要', '周虹宏', '记录实训研发各阶段 5 次关键技术决策例会纪要与行动项跟踪', [
    [
        H('1 第 1 次项目例会纪要（先启阶段：项目启动与团队分工）'),
        T(['会议要素', '纪要详细内容'], [
            ['会议时间与地点', '2026年08月31日 14:00-16:00 | 三创谷实训基地 612 实训室'],
            ['参会人员与主持', '参会人员：周虹宏、钟毓林、胡吉涵、李治宏；指导教师：钟辉；主持人：周虹宏'],
            ['会议核心议题', '明确《企业级框架技术开发》实训项目选题、团队职责分工、研发生命周期与技术基线'],
            ['会议研讨与决议', '1. 正式确立项目选题为“智慧医养大数据决策分析系统”，立足医养结合养老管理实际痛点。\n2. 团队组织分工明确：周虹宏担任项目组长兼 QA 质量负责人；钟毓林负责后端认证中心、系统管理与健康随访；胡吉涵负责底层数据架构、设备接入与围栏算法；李治宏负责 Vue 3 前端工程与交互大盘。\n3. 技术栈统一基线：Java 17 + Spring Boot 3.5.16 + MyBatis-Plus + MySQL 8.0 + Vue 3 + Element Plus。'],
            ['后续行动项 (Action)', '周虹宏负责起草《01 软件需求规范说明书》初稿；胡吉涵完成 MySQL 初始化环境准备。']
        ]),
        H('2 第 2 次项目例会纪要（精化阶段：架构设计与数据库评审）'),
        T(['会议要素', '纪要详细内容'], [
            ['会议时间与地点', '2026年09月02日 15:00-17:30 | 三创谷实训基地 612 实训室'],
            ['参会人员与主持', '参会人员：周虹宏、钟毓林、胡吉涵、李治宏；指导教师：钟辉；主持人：周虹宏'],
            ['会议核心议题', '评审系统架构 4+1 视图、冻结数据库 18 张表设计基线、确立统一 RESTful 接口契约'],
            ['会议研讨与决议', '1. 评审并通过系统五层分层逻辑架构与 4+1 视图模型，确认采用前后端同源 Fat-JAR 交付模式。\n2. 评审并通过 18 张核心数据库表结构设计；针对老人换佩戴腕表的业务场景，确认采用 device_binding 历史关联表进行物理隔离与时间线解耦，杜绝轨迹归属串人。\n3. 统一前后端 API 数据通信契约格式为 Api.Result<T>，所有 Long 型主键向前端输出序列化为字符串，规避 JavaScript 浮点数精度截断问题。'],
            ['后续行动项 (Action)', '钟毓林负责实现认证与权限拦截器；胡吉涵建立 database/01-schema.sql 数据库基线脚本。']
        ])
    ],
    [
        H('3 第 3 次项目例会纪要（构建前中期：接口联调与权限攻坚）'),
        T(['会议要素', '纪要详细内容'], [
            ['会议时间与地点', '2026年09月04日 16:00-18:00 | 三创谷实训基地 612 实训室'],
            ['参会人员与主持', '参会人员：周虹宏、钟毓林、胡吉涵、李治宏；主持人：周虹宏'],
            ['会议核心议题', '前后端第一阶段集成联调、Spring Security 过滤链排错与设备标准接入协议设计'],
            ['会议研讨与决议', '1. 解决开发态 Vite 跨域代理与生产态 CSRF 校验策略问题，确保管理端登录注销与权限校验平稳生效。\n2. 攻克设备端通信协议：穿戴设备缺乏浏览器 Cookie 与 CSRF 机制，确立在请求头中使用 X-Device-Key 进行 SHA-256 签名鉴权，明文密钥仅在管理员签发时一次性展示。\n3. 确立老人档案误录受限物理删除与常规软归档的业务边界，对存在体检或绑定关系的老人禁止物理删除，仅允许软归档。'],
            ['后续行动项 (Action)', '李治宏完成 SimulationPanel 轨迹模拟注入面板；胡吉涵完成设备接入控制器开发。']
        ]),
        H('4 第 4 次项目例会纪要（构建后期：高并发压测与缺陷复盘）'),
        T(['会议要素', '纪要详细内容'], [
            ['会议时间与地点', '2026年09月08日 16:00-18:00 | 三创谷实训基地 612 实训室'],
            ['参会人员与主持', '参会人员：周虹宏、钟毓林、胡吉涵、李治宏；指导教师：钟辉；主持人：周虹宏'],
            ['会议核心议题', '双十万级高并发负载压力测试结果评审、典型缺陷（D01~D03）闭环复盘'],
            ['会议研讨与决议', '1. 评审双十万级数据性能压测报告：在 20 并发持续 60 秒下，四核心端点 P95 响应时间均在 1.15 秒内，错误率保持 0.00%，达到严苛的性能准出要求。\n2. 深入复盘 D01~D03 缺陷闭环过程：确认 MySQL 保留字已全部加反引号转义；确认乐观锁机制有效防范了老人档案并发覆写；确认围栏告警状态机成功具备了时序乱序幂等性。\n3. 确认 28 项 JUnit 自动化测试用例全部绿灯通过。'],
            ['后续行动项 (Action)', '周虹宏更新《03 系统测试日志》；全员转入全套交付文档编制与答辩准备。']
        ])
    ],
    [
        H('5 第 5 次项目例会纪要（产品化收尾：交付文档审查与答辩预演）'),
        T(['会议要素', '纪要详细内容'], [
            ['会议时间与地点', '2026年09月10日 09:30-11:30 | 三创谷实训基地 612 实训室'],
            ['参会人员与主持', '参会人员：周虹宏、钟毓林、胡吉涵、李治宏；指导教师：钟辉；主持人：周虹宏'],
            ['会议核心议题', '全套 10 份工程交付文档一致性审查、答辩演示演练与实训总结'],
            ['会议研讨与决议', '1. 全员交叉复核 10 份交付文档与代码实现的一致性，核验通过《项目报告一致性清单》，保证各文档数据、图表与学号信息完全对应。\n2. 演练项目结项答辩 PPT 与系统演示：周虹宏汇报项目背景、团队管理与质量结论；钟毓林汇报后端架构、安全鉴权与随访业务；胡吉涵汇报 18 表数据架构、设备接入与围栏算法；李治宏汇报前端 Vue 3 交互与大屏数据模拟可视化。\n3. 确认系统具备结项验收条件，全套技术资料归档。'],
            ['后续行动项 (Action)', '全员提交实训报告与全套工程交付物，迎接 9 月 11 日结项答辩。']
        ])
    ]
])

# ==============================================================================
# 08 系统详细设计说明书
# ==============================================================================
add('08', '系统详细设计说明书', '钟毓林、胡吉涵、李治宏', '阐述接口规范、状态机流转、Haversine 测距算法、设备接入幂等性及前端核心组件实现', [
    [
        H('1 通用 RESTful 接口规范与数据契约'),
        P('系统后端对管理端暴露以 /api/v1 为前缀的 RESTful 接口，对外部设备暴露以 /api/device/v1 为前缀的硬件接入接口。网络通信统一采用 UTF-8 编码的 JSON 数据格式。接口响应严格封装在统一泛型实体 Api.Result<T> 中：'),
        T(['返回字段', '物理类型', '约束与设计说明'], [
            ['code', 'String', '状态结果码。操作成功统一返回 "OK"；业务异常返回对应大写下划线业务错误码（如 "STALE_UPDATE"）'],
            ['message', 'String', '面向用户的提示消息文本。操作成功返回 "成功"；失败返回友好的中文业务异常原因'],
            ['data', 'Object / Array', '业务响应数据载荷。空结果时输出 null；列表数据直接输出 JSON 数组'],
            ['requestId', 'String', '全链路请求追踪唯一跟踪 ID，用于生产排错与日志定位（基于 UUID 生成）'],
            ['timestamp', 'String', '服务端处理完成的带时区 ISO 8601 标准时间戳（如 2026-09-09T14:30:00.123+08:00）']
        ]),
        H('2 接口权限分类与安全过滤链设计'),
        P('系统基于 Spring Security 构建了双通道安全过滤链：后台接口基于 Session 与 CSRF 保护；设备接口通过独立设备密钥校验：'),
        T(['接口路径分类', '支持 HTTP 方法', '访问鉴权机制', '业务责任与设计约束'], [
            ['/api/v1/auth/*', 'GET, POST', '匿名或会话认证', '负责获取 CSRF Token、账号登录、获取当前登录信息及销毁会话注销'],
            ['/api/v1/dashboard, /statistics/*', 'GET', '三角色鉴权通过', '运营决策大盘及人口、健康、随访统计指标查询，支持参数下钻'],
            ['/api/v1/elders, /health-records 等', 'POST, PUT, GET', 'ADMIN / OPERATOR', '日常业务建档、修改与流转；写入操作均要求携带动态 CSRF Token'],
            ['/api/v1/elders/{id}', 'DELETE', '仅系统管理员 ADMIN', '仅允许物理删除无任何关联业务记录的误录档案，其余只能软归档'],
            ['/api/v1/demo/*', 'POST', '仅管理员且在 demo 环境', '提供轨迹模拟与批量注入，非 demo 环境直接返回 404 Not Found'],
            ['/api/device/v1/locations', 'POST', '携带 X-Device-Key', '标准穿戴设备批量 GPS 上报，支持 1~500 点高效写入与整批幂等'],
            ['/api/device/v1/heartbeats', 'POST', '携带 X-Device-Key', '穿戴设备在线心跳上报，基于 (device_id, event_id) 持久化幂等']
        ])
    ],
    [
        H('3 核心业务领域状态流转与并发控制设计'),
        P('3.1 老人健康档案生命周期与乐观锁并发控制：老人档案状态包含在册（ACTIVE）与归档（ARCHIVED）。针对医养机构多端并发编辑老人档案的业务冲突问题，在 elder 表中引入 version 字段。当客户端发起更新时，SQL 自动附带 WHERE id = #{id} AND version = #{version}；若版本已被他人抢先更新，MyBatis-Plus 乐观锁拦截器抛出业务异常，通知前端提示用户刷新最新数据后再试，杜绝静默脏写。'),
        P('3.2 家庭医生签约与随访履约闭环：随访计划具备到期应访、履约完成与因故取消三种状态。单项随访计划严格受声明式事务（@Transactional）保护，仅允许执行一次完成操作，完成时同步记录医生签字结果与随访小结，确保履约数据的司法合规性。'),
        H('4 空间地理计算与电子围栏告警状态机算法'),
        P('4.1 Haversine 大圆航线空间距离算法：系统采用 WGS84 椭球体几何投影模型，解算智能穿戴腕表经纬度 (lat1, lon1) 与电子围栏中心点 (lat2, lon2) 之间的地表大圆弧线距离 d：'),
        P('设 Δlat = lat2 - lat1，Δlon = lon2 - lon1，a = sin²(Δlat/2) + cos(lat1)·cos(lat2)·sin²(Δlon/2)，c = 2·atan2(√a, √(1-a))，则空间距离 d = R·c（其中地球半径 R 取 6371000 米）。当 d > radius_m 时判定为越界离开，否则判定在围栏内部。'),
        P('4.2 围栏告警防抖有限状态机（FSM）跃迁机制：为杜绝 GPS 信号漂移造成的告警风暴，状态机遵循以下严密跃迁矩阵：'),
        T(['前置状态', '输入新定位点判据', '状态机动作与数据库事务处理', '最终跃迁状态'], [
            ['内部（SAFE）或未知', '新定位点判定在围栏外', '生成一条待处置越界告警事件（geofence_alert），状态机绑定该告警事件', '越界告警（ALERT）'],
            ['越界告警（ALERT）', '持续收到围栏外新定位', '更新状态机 last_point_id，绝不重复生成告警事件，阻断告警风暴', '维持越界（ALERT）'],
            ['越界告警（ALERT）', '新定位点重新回到围栏内', '更新当前告警事件的 returned_at 归巢时间与闭环原因，解绑活动告警', '恢复正常（SAFE）'],
            ['任意状态', '收到迟到历史定位（时间戳更旧）', '定位流水正常入库留痕，但不推动当前围栏状态机跃迁，防状态回退颠簸', '维持原状态']
        ])
    ],
    [
        H('5 智能设备安全接入凭证与通信幂等性设计'),
        P('5.1 设备专属凭据与单向哈希存储：每台智能腕表出厂入库后由管理员在后台签发独立通信密钥。密钥由系统生成 32 字节密码学强随机数，明文仅在签发时向管理员一次性展示；数据库 device_credential 表仅持久化加盐 SHA-256 摘要（key_hash）。设备端请求在 HTTP 请求头携带 X-Device-Key，服务端通过比对摘要完成鉴权，支持密钥即时轮换与旧凭证一秒失效。'),
        P('5.2 基于事件唯一编号的持久化幂等性：智能穿戴设备由于弱网容易发起网络超时重试。系统在 location_point 与 device_heartbeat_event 表中均建立了 (device_id, event_id) 唯一索引。当设备原样重传相同事件时，服务端捕获唯一键冲突并识别为重复上报，直接返回成功响应；若客户端使用已有 eventId 篡改时间或坐标重新提交，系统强行抛出 HTTP 409 业务冲突异常，杜绝伪造数据。'),
        H('6 前端工程架构与核心组件详细实现'),
        P('6.1 Vue 3 组合式架构：前端采用 Vue 3 Composition API + Pinia 状态管理模式，构建了模块化清晰的业务前端体系。App.vue 负责全局路由拓扑与多角色菜单动态渲染。'),
        P('6.2 GeoCanvas 自适应矢量坐标画布：针对脱网与教学演示场景，前端自主研发了轻量级离线自适应矢量经纬度坐标投影画布组件。该组件能够动态计算查询所得轨迹点集的最大最小经纬度包围盒（Bounding Box），自动计算缩放比例与中心偏移量，将 WGS84 经纬度无损映射至 HTML5 Canvas 像素坐标，实现高流畅度的轨迹回放、速度调节与 10 分钟时序断点缺口高亮提示。'),
        P('6.3 SimulationPanel 空间数据模拟引擎：为方便机构演示与测试，前端提供了 SimulationPanel 面板，内置单点微调、直线折线巡视、闭合环形巡逻以及出圈越界返回四大轨迹数学模型，前端生成后即时在画布中呈现矢量预览，确认后一键批量注入后台。')
    ]
])

# ==============================================================================
# 10 项目组成员分工
# ==============================================================================
add('10', '项目组成员分工', '周虹宏', '明确项目团队组织架构、四人岗位职责、任务分解结构 WBS、交叉复核机制及答辩分工', [
    [
        H('1 团队组织架构与角色定位'),
        P('本项目团队由成都理工大学计算机与网络安全学院 2024 级软件工程专业四名同学组成。团队按照现代软件工程项目团队模型组建，职责划分清晰，相互协同互信：'),
        T(['成员姓名与学号', '团队岗位角色', '主要负责领域与交付成果清单'], [
            ['周虹宏（202417020210）', '项目经理兼质量负责人 (PM & QA)', '负责统筹软件需求规格说明、制定整体测试计划、组织敏捷例会、编制测试用例与测试日志、跟踪缺陷闭环、统筹结项答辩汇报。'],
            ['钟毓林（202417020128）', '后端开发工程师 (Backend Dev)', '负责系统安全鉴权中心（Spring Security CSRF/RBAC）、系统管理模块、老人健康档案与体检管理、家庭医生签约随访履约业务逻辑及核心单元测试。'],
            ['胡吉涵（202417020202）', '后端兼数据测试工程师 (Data & Backend)', '负责 18 张数据表物理架构设计与优化、智能设备安全凭据体系、批量 GPS 接入、空间电子围栏与轨迹算法、双十万级高并发负载压力测试及部署说明书。'],
            ['李治宏（202417020205）', '前端开发工程师 (Frontend Dev)', '负责 Vue 3 组合式架构搭建、Element Plus 企业级组件交互设计、综合运营大屏大盘、GeoCanvas 离线坐标画布、SimulationPanel 轨迹模拟引擎及端端测试。']
        ]),
        H('2 工作任务分解结构（WBS）与责任矩阵（RACI）'),
        P('项目各项核心工程任务严格实行责任到人、全程留痕的 RACI 责任管理体系：'),
        T(['项目交付物与工作任务', '负责人 (R)', '审核人 (A)', '协助人 (C)', '知悉人 (I)'], [
            ['01 软件需求规范说明书', '周虹宏', '周虹宏', '钟毓林、胡吉涵、李治宏', '指导老师'],
            ['02 数据库设计说明书', '胡吉涵', '钟毓林', '周虹宏', '全员'],
            ['03 系统测试日志与复盘', '周虹宏', '周虹宏', '全员各自模块', '指导老师'],
            ['04 系统架构设计说明书', '钟毓林、胡吉涵', '周虹宏', '李治宏', '指导老师'],
            ['05 软件测试用例说明书', '周虹宏', '周虹宏', '全员执行', '指导老师'],
            ['06 测试计划说明书', '周虹宏', '周虹宏', '胡吉涵', '全员'],
            ['07 项目例会纪要', '周虹宏', '周虹宏', '全员参会', '指导老师'],
            ['08 系统详细设计说明书', '钟毓林、胡吉涵、李治宏', '周虹宏', '全员', '指导老师'],
            ['10 项目组成员分工说明书', '周虹宏', '周虹宏', '全员确认', '指导老师'],
            ['11 项目部署环境说明书', '胡吉涵', '钟毓林', '周虹宏、李治宏', '指导老师']
        ])
    ],
    [
        H('3 团队协同开发规范与代码审查机制'),
        P('1. 契约优先原则：前后端开发严格遵循“接口契约先行”原则。在编码前首先明确 API 路径、请求参数、字段校验边界与统一响应实体，形成设计基线后再并行开发。'),
        P('2. Git 分支管理与代码审查（Code Review）：项目采用标准 Git Feature 分支开发模式。功能代码提交必须附带关联的需求编号与本地自动化测试执行结果，经组内交叉代码审查（Review）通过后方可合并至主干。'),
        P('3. 数据库变更控制规范：禁止直接在个人开发工具中手动篡改他人测试数据库。任何表结构调整必须输出标准 SQL 变更说明与可回滚脚本，确保团队环境高度一致。'),
        P('4. 凭证脱敏与安全底线：设备专属通信密钥、数据库敏感账号与管理员密码严禁明文硬编码进源码或公开提交至代码仓库，确保系统工程安全合规。'),
        H('4 结项答辩分工与演示汇报规划'),
        P('根据三创谷实训项目结项答辩要求，答辩委员会汇报演示按照角色进行系统化分工：'),
        T(['答辩汇报成员', '答辩重点讲解模块与业务展示', '演示角色与展示亮点', '汇报时间建议'], [
            ['周虹宏（组长/PM）', '项目宏观背景、需求全景矩阵、敏捷项目过程管理、测试准出结论', '演示系统登录、RBAC 细粒度权限控制与退出销毁', '3 分钟'],
            ['钟毓林（后端）', 'Spring Boot 3 架构、Session+CSRF 过滤链、老人档案、健康与随访', '演示老人建档、体检异常快照、家庭医生履约闭环', '3 分钟'],
            ['胡吉涵（数据/算法）', '18 表架构、设备安全凭据体系、Haversine 测距与围栏防抖状态机、并发压测', '演示设备配对、独立密钥上报、双十万级压测指标', '3 分钟'],
            ['李治宏（前端）', 'Vue 3 组合式架构、综合运营决策大盘、GeoCanvas 离线矢量画布、轨迹模拟', '演示运营大盘交互、轨迹模拟一键注入、时序回放', '3 分钟']
        ])
    ]
])

# ==============================================================================
# 11 项目部署环境说明书
# ==============================================================================
add('11', '项目部署环境说明书', '胡吉涵主责，钟毓林协助', '说明系统软硬件运行环境基准、配置参数字典、初始化脚本执行、打包部署与运维手册', [
    [
        H('1 部署架构概述与软硬件环境基准'),
        P('智慧医养大数据决策分析系统采用现代前后端同源集成的 Spring Boot 可执行 Fat-JAR 交付形态，前端构建产物内嵌于后端静态资源目录中，支持单实例一键部署运行。底层依赖一套 MySQL 8.0 关系型数据库。软硬件部署基准配置如下：'),
        T(['系统组件维度', '推荐生产运行基准', '实训开发与测试验证基准'], [
            ['操作系统', 'Ubuntu 22.04 LTS / CentOS 7.9 / Windows Server', 'Windows 11 x86_64 企业级开发环境'],
            ['CPU 与内存', '推荐 4 核 CPU 以上，8 GB 内存以上，配置高速 SSD', '4 核 CPU，16 GB 内存（实训开发机）'],
            ['Java 运行时', 'OpenJDK 17 LTS / Eclipse Temurin 17.0.12', 'Java 17 生产兼容编译目标，通过 Maven 3.9 构建'],
            ['关系型数据库', 'MySQL 8.0.16 及以上（实测 8.0.46），utf8mb4 字符集', 'MySQL 8.0.46，统一配置 Asia/Shanghai 本地时区'],
            ['前端运行环境', '无需独立 Node.js 服务（构建后静态资源内置于 JAR）', '编译期依赖 Node.js 22.12.0 及 Vite 7.3.6 构建工具'],
            ['服务端口规划', '应用主服务默认 8080 端口（可配置）；数据库 3306 端口', '本地开发采用 8080，隔离测试验收采用 18081 与 13317 端口'],
            ['终端浏览器', 'Google Chrome 120+、Microsoft Edge 等现代浏览器', 'Chrome 152 自动化测试环境全部通过']
        ]),
        H('2 数据库初始化与升级规范'),
        P('1. 全新数据库初始化：在 MySQL 8.0 实例中，首先使用具备 DBA 权限的账号执行 database/00-create-database.sql 建立 smart_care 数据库并赋予专用连接用户权限；随后执行 database/01-schema.sql 顺序初始化全部 18 张表结构、外键与联合索引。'),
        P('2. 权限隔离原则：生产环境下应用数据库账号仅授予 SELECT、INSERT、UPDATE、DELETE 数据操作权限，禁止授予 DROP 等破坏性 DDL 权限，确保底层数据库安全。'),
        P('3. 时区配置强一致：数据库服务器时区必须明确设置为 +08:00，JDBC 连接串必须明确声明 connectionTimeZone=Asia/Shanghai，杜绝跨时区导致的体检与定位时间漂移。')
    ],
    [
        H('3 关键环境变量与核心配置字典'),
        P('系统遵循 12-Factor 现代化云原生配置原则，核心配置支持通过系统环境变量解耦注入：'),
        T(['环境变量名称', '默认值或配置示例', '配置语义与安全加固说明'], [
            ['DB_URL', 'jdbc:mysql://127.0.0.1:3306/smart_care?useUnicode=true&characterEncoding=UTF-8&connectionTimeZone=Asia/Shanghai', '数据库 JDBC 连接串，必须显式声明编码与上海时区'],
            ['DB_USERNAME', 'smart_care_app', '应用连接专用数据库操作账号，生产严禁使用 root'],
            ['DB_PASSWORD', '自定义强密码（交互式配置）', '数据库账号访问密码，严禁明文硬编码至配置文件'],
            ['APP_ADMIN_PASSWORD', '自定义管理员初始化密码', '仅首次空库创建 admin 时生效，要求 12~72 字符，包含大小写与数字'],
            ['SERVER_PORT', '8080', 'Spring Boot 应用监听端口，可按环境需要灵活重定向'],
            ['SPRING_PROFILES_ACTIVE', 'demo（演示）/ local（本地）/ prod（生产）', '运行 Profile 模式，demo 开放模拟入口，prod 强制安全加固'],
            ['APP_SEED_DEMO', 'true / false', '仅 demo 环境生效，首次启动时自动填充基础演示数据']
        ]),
        H('4 系统一键构建与打包流程'),
        P('项目根目录下提供了自动化打包脚本 scripts/build.ps1，在 PowerShell 中执行该脚本即可完成端到端自动化编译打包：'),
        P('1. 脚本首先进入 frontend 目录执行 npm ci 与 npm run build，通过 Vite 7 编译生成优化压缩后的前端静态文件；'),
        P('2. 将前端 dist 产物自动化复制至后端 src/main/resources/static 目录中；'),
        P('3. 调用 Maven Wrapper（mvnw.cmd）执行标准构建打包，全量运行 28 项自动化测试；'),
        P('4. 在 target 目录下生成开箱即用的可执行文件 dev-0.0.1-SNAPSHOT.jar，实现零依赖一键运行。')
    ],
    [
        H('5 启动运行与常用 Profile 配置模式'),
        P('1. 演示模式启动（面向实训结项答辩与现场演示）：'),
        P('执行命令：.\\scripts\\start.ps1 -Profile demo -Port 8080。该模式会自动初始化内置示例老人、健康体检与围栏数据，并开放 SimulationPanel 轨迹模拟注入工具，方便现场交互演示。'),
        P('2. 生产模式启动（面向机构正式部署）：'),
        P('执行命令：java -Dspring.profiles.active=prod -jar target/dev-0.0.1-SNAPSHOT.jar。生产模式下自动屏蔽任何模拟注入端点，强制启用严格的 HTTPS Cookie 安全策略与敏感信息审计日志。'),
        H('6 常见部署故障与应急排查手册'),
        P('测试与运维团队总结了常见的部署环境故障与标准应对处置方案：'),
        T(['故障现象', '常见根因分析', '标准处置与排查步骤'], [
            ['启动提示缺少 APP_ADMIN_PASSWORD', '首次初始化空库时未提供管理员初始密码', '在环境变量中显式配置满足长度复杂度要求的 APP_ADMIN_PASSWORD 后重新启动'],
            ['Access denied for user 连接失败', '数据库账号密码错误或未授予远程授权', '核对 DB_USERNAME 与密码，检查 MySQL 用户主机白名单（如 % 或 localhost）'],
            ['Web 端口 8080 Address already in use', '8080 端口已被本机其他后台服务占用', '修改环境变量 SERVER_PORT=18080，使用备用端口启动应用'],
            ['JAR 文件重命名失败（Text file busy）', 'Windows 系统锁定了正在运行中的旧 JAR', '在任务管理器中正常终止原 Java 进程，再执行构建打包'],
            ['设备定位接口返回 401 Unauthorized', '穿戴设备未在请求头携带合法 X-Device-Key', '在管理员腕表管理界面重新签发密钥，并将新密钥配置进硬件网关'],
            ['历史健康图表时间显示异常', '数据库时区与应用系统时区未对齐', '检查 MySQL 数据库参数 time_zone，并在 DB_URL 中声明 Asia/Shanghai']
        ])
    ]
])

# ==============================================================================
# DOCX 排版与生成引擎
# ==============================================================================
def setfont(run, size=None, bold=None, color='000000', east_asia='宋体'):
    run.font.name = 'Times New Roman'
    run._element.get_or_add_rPr().get_or_add_rFonts().set(qn('w:eastAsia'), east_asia)
    if size is not None: run.font.size = Pt(size)
    if bold is not None: run.bold = bold
    if color: run.font.color.rgb = RGBColor.from_string(color)

def set_shading(cell, fill):
    tcpr = cell._tc.get_or_add_tcPr()
    shd = tcpr.find(qn('w:shd'))
    if shd is None:
        shd = OxmlElement('w:shd'); tcpr.append(shd)
    shd.set(qn('w:fill'), fill)

def set_cell_margins(cell, top=70, bottom=70, left=90, right=90):
    tcpr = cell._tc.get_or_add_tcPr()
    margins = tcpr.find(qn('w:tcMar'))
    if margins is None:
        margins = OxmlElement('w:tcMar'); tcpr.append(margins)
    for edge, value in (('top', top), ('bottom', bottom), ('left', left), ('right', right)):
        node = margins.find(qn('w:' + edge))
        if node is None:
            node = OxmlElement('w:' + edge); margins.append(node)
        node.set(qn('w:w'), str(value)); node.set(qn('w:type'), 'dxa')

def set_table_borders(tbl, color='B7B7B7', size='6'):
    tblpr = tbl._tbl.tblPr
    borders = tblpr.find(qn('w:tblBorders'))
    if borders is None:
        borders = OxmlElement('w:tblBorders'); tblpr.append(borders)
    for edge in ('top', 'left', 'bottom', 'right', 'insideH', 'insideV'):
        node = borders.find(qn('w:' + edge))
        if node is None:
            node = OxmlElement('w:' + edge); borders.append(node)
        node.set(qn('w:val'), 'single'); node.set(qn('w:sz'), size); node.set(qn('w:color'), color)

def no_table_borders(tbl):
    tblpr = tbl._tbl.tblPr
    borders = tblpr.find(qn('w:tblBorders'))
    if borders is None:
        borders = OxmlElement('w:tblBorders'); tblpr.append(borders)
    for edge in ('top', 'left', 'bottom', 'right', 'insideH', 'insideV'):
        node = OxmlElement('w:' + edge); node.set(qn('w:val'), 'nil'); borders.append(node)

def paragraph(container, text, style=None, size=10.5, align=None, bold=None, east_asia=None):
    p = container.add_paragraph(style=style) if style else container.add_paragraph()
    p.paragraph_format.space_after = Pt(6); p.paragraph_format.line_spacing = 1.35
    if align is not None: p.alignment = align
    grid = OxmlElement('w:snapToGrid'); grid.set(qn('w:val'), '0'); p._p.get_or_add_pPr().append(grid)
    for lineidx, line in enumerate(str(text).split('\n')):
        if lineidx: p.add_run().add_break()
        r = p.add_run(line); setfont(r, size, bold, east_asia=east_asia or ('黑体' if style in ('Title', 'Heading 1', 'Heading 2') else '宋体'))
    return p

def table(container, heads, rows):
    tbl = container.add_table(rows=1, cols=len(heads)); tbl.alignment = WD_TABLE_ALIGNMENT.CENTER; tbl.autofit = True
    set_table_borders(tbl)
    for row in rows: tbl.add_row()
    for ri, row in enumerate([heads] + rows):
        tr = tbl.rows[ri]._tr; props = tr.get_or_add_trPr(); props.append(OxmlElement('w:cantSplit'))
        if ri == 0: props.append(OxmlElement('w:tblHeader'))
        for ci, value in enumerate(row):
            cell = tbl.cell(ri, ci); cell.text = str(value); cell.vertical_alignment = WD_CELL_VERTICAL_ALIGNMENT.CENTER; set_cell_margins(cell)
            if ri == 0: set_shading(cell, 'D9D9D9')
            elif ri % 2 == 0: set_shading(cell, 'F7F7F7')
            for p in cell.paragraphs:
                p.paragraph_format.space_before = Pt(1); p.paragraph_format.space_after = Pt(1); p.paragraph_format.line_spacing = 1.15
                if ci == 0 and len(heads) <= 3: p.alignment = WD_ALIGN_PARAGRAPH.CENTER
                for r in p.runs: setfont(r, 9, ri == 0, east_asia='黑体' if ri == 0 else '宋体')
    paragraph(container, '', size=2).paragraph_format.space_after = Pt(0)
    return tbl

def configure_document(doc):
    sec = doc.sections[0]; sec.page_width = Cm(21); sec.page_height = Cm(29.7)
    sec.top_margin = Cm(2.54); sec.bottom_margin = Cm(2.54); sec.left_margin = Cm(2.54); sec.right_margin = Cm(2.54)
    sec.header_distance = Cm(1.25); sec.footer_distance = Cm(1.25)
    normal = doc.styles['Normal']; normal.font.name = 'Times New Roman'; normal.font.size = Pt(10.5); normal._element.rPr.rFonts.set(qn('w:eastAsia'), '宋体')
    for name, size in (('Title', 26), ('Heading 1', 16), ('Heading 2', 13)):
        style = doc.styles[name]; style.font.name = 'Times New Roman'; style.font.size = Pt(size); style.font.bold = True; style.font.color.rgb = RGBColor(0, 0, 0); style._element.rPr.rFonts.set(qn('w:eastAsia'), '黑体')
        style.paragraph_format.keep_with_next = True
        ppr = style._element.get_or_add_pPr(); border = ppr.find(qn('w:pBdr'))
        if border is not None: ppr.remove(border)
    settings = doc.settings.element
    update = OxmlElement('w:updateFields'); update.set(qn('w:val'), 'true'); settings.append(update)
    footer = sec.footer.paragraphs[0]; footer.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r = footer.add_run(PROJECT + '  ' + VERSION + '  '); setfont(r, 8, color='666666')
    fld = OxmlElement('w:fldSimple'); fld.set(qn('w:instr'), 'PAGE'); footer._p.append(fld)

def add_cover(doc, spec):
    outer = doc.add_table(rows=1, cols=2); outer.alignment = WD_TABLE_ALIGNMENT.CENTER; outer.autofit = False; no_table_borders(outer)
    outer.columns[0].width = Cm(2.5); outer.columns[1].width = Cm(13.2)
    row = outer.rows[0]; row.height = Cm(21.8); row.height_rule = WD_ROW_HEIGHT_RULE.EXACTLY
    left, right = row.cells; left.width = Cm(2.5); right.width = Cm(13.2)
    set_shading(left, 'D9D9D9'); left.vertical_alignment = WD_CELL_VERTICAL_ALIGNMENT.BOTTOM; right.vertical_alignment = WD_CELL_VERTICAL_ALIGNMENT.TOP
    set_cell_margins(left, 100, 160, 110, 110); set_cell_margins(right, 20, 20, 240, 80)
    lp = left.paragraphs[0]; lp.alignment = WD_ALIGN_PARAGRAPH.LEFT
    lr = lp.add_run(f'分  类：项目交付文档\n使用者：项目组\n文档编号：\n{PROJECT_NO}-{spec["number"]}\n\n{DEPARTMENT}')
    setfont(lr, 7.5, east_asia='宋体')
    right.paragraphs[0].paragraph_format.space_after = Pt(0)
    top = right.add_table(rows=3, cols=2); top.alignment = WD_TABLE_ALIGNMENT.RIGHT; top.autofit = False; set_table_borders(top, '8A8A8A', '5')
    for i, label in enumerate(('卷  号', '卷内编号', '密  级')):
        top.cell(i, 0).text = label; top.cell(i, 1).text = ''
        top.cell(i, 0).width = Cm(2.1); top.cell(i, 1).width = Cm(3.6)
        for c in top.rows[i].cells:
            set_cell_margins(c, 20, 20, 50, 50)
            for p in c.paragraphs:
                p.alignment = WD_ALIGN_PARAGRAPH.CENTER
                for r in p.runs: setfont(r, 7.5, bold=(c is top.cell(i, 0)), east_asia='宋体')
    for _ in range(4): paragraph(right, '', size=4).paragraph_format.space_after = Pt(6)
    p = paragraph(right, '智慧医养大数据公共服务平台\n大数据决策分析系统', size=19, align=WD_ALIGN_PARAGRAPH.CENTER, bold=True, east_asia='黑体'); p.paragraph_format.space_after = Pt(20)
    p = paragraph(right, spec['title'], style='Title', size=25, align=WD_ALIGN_PARAGRAPH.CENTER, bold=True, east_asia='黑体'); p.paragraph_format.space_after = Pt(28)
    for label, value in (('项 目 承 担 部 门', DEPARTMENT), ('撰 写 人（签名）', spec['owner']), ('完 成 日 期', DATE), ('本文档使用部门', '■项目组  □主管领导  □维护人员  □用户'), ('评审负责人（签名）', '周虹宏'), ('评 审 日 期', DATE)):
        p = paragraph(right, f'{label}：  {value}', size=10.5, east_asia='宋体'); p.paragraph_format.space_after = Pt(8)
    doc.add_page_break()

def add_control_pages(doc, spec):
    p = paragraph(doc, '文档信息', style='Heading 1', size=16, align=WD_ALIGN_PARAGRAPH.CENTER, bold=True, east_asia='黑体'); p.paragraph_format.space_after = Pt(14)
    table(doc, ['文档信息', '内容'], [['标题', PROJECT + ' ' + spec['title']], ['作者', spec['owner']], ['创建日期', DATE], ['上次更新日期', DATE], ['版本', VERSION], ['部门名称', DEPARTMENT], ['适用范围', spec['scope']]])
    p = paragraph(doc, '修订文档历史记录', style='Heading 1', size=16, align=WD_ALIGN_PARAGRAPH.CENTER, bold=True, east_asia='黑体'); p.paragraph_format.space_before = Pt(18)
    table(doc, ['日期', '版本', '说明', '作者'], [[DATE, VERSION, '按照三创谷实训项目交付规范编制，经全员交叉评审并与实际系统代码验证基线同步', '软件工程实训项目组']])
    doc.add_page_break()
    p = paragraph(doc, '目  录', style='Heading 1', size=16, align=WD_ALIGN_PARAGRAPH.CENTER, bold=True, east_asia='黑体'); p.paragraph_format.space_after = Pt(14)
    seen = set()
    for page in spec['pages']:
        for block in page:
            if block[0] != 'h' or block[1] in seen: continue
            seen.add(block[1]); entry = paragraph(doc, block[1], size=10.5, east_asia='宋体')
            entry.paragraph_format.left_indent = Cm(0.65 if re.match(r'^\d+\.\d+', block[1]) else 0)
            entry.paragraph_format.space_after = Pt(4)

def add_figure(doc, path, caption, width):
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER; p.paragraph_format.keep_with_next = True
    p.add_run().add_picture(str(ROOT / path), width=Inches(width))
    cp = paragraph(doc, caption, size=9, align=WD_ALIGN_PARAGRAPH.CENTER, east_asia='宋体'); cp.paragraph_format.keep_with_next = True; cp.paragraph_format.space_after = Pt(10)

def build(spec):
    doc = Document(); configure_document(doc); add_cover(doc, spec); add_control_pages(doc, spec)
    for page in spec['pages']:
        first_block = True
        for block in page:
            if block[0] == 'h':
                p = paragraph(doc, block[1], 'Heading 1', 16, bold=True, east_asia='黑体'); p.paragraph_format.space_before = Pt(8); p.paragraph_format.space_after = Pt(7); p.paragraph_format.keep_with_next = True
                if first_block: p.paragraph_format.page_break_before = True
            elif block[0] == 'p': paragraph(doc, block[1])
            elif block[0] == 't': table(doc, block[1], block[2])
            elif block[0] == 'img': add_figure(doc, block[1], block[2], block[3])
            first_block = False
    doc.core_properties.title = PROJECT + ' ' + spec['title']
    doc.core_properties.author = '软件工程实训项目组'
    doc.core_properties.subject = spec['scope']
    doc.core_properties.comments = '依据三创谷实训项目规范编制，内容与当前系统代码实现和工程质量证据保持严格一致。'
    out = OUT / (spec['number'] + ' ' + spec['title'] + '.docx')
    doc.save(out)
    md = ['# ' + spec['title'], '版本 ' + VERSION + '　' + DATE]
    for page in spec['pages']:
        for b in page:
            if b[0] == 'h': md += ['', '## ' + b[1]]
            elif b[0] == 'p': md += ['', b[1]]
            elif b[0] == 't': md += ['', '| ' + ' | '.join(b[1]) + ' |', '| ' + ' | '.join(['---'] * len(b[1])) + ' |'] + ['| ' + ' | '.join(map(str, r)) + ' |' for r in b[2]]
            elif b[0] == 'img': md += ['', f'![{b[2]}]({b[1]})', '', b[2]]
    (SOURCE / (spec['number'] + '-' + spec['title'] + '.md')).write_text('\n'.join(md) + '\n', encoding='utf-8')
    print(out.name)

import sys
for spec in docs:
    if len(sys.argv) == 1 or spec['number'] in sys.argv[1:]:
        build(spec)
(ROOT / 'docs/.qa').mkdir(parents=True, exist_ok=True); (ROOT / 'docs/.qa/document-manifest.json').write_text(json.dumps([{'file': s['number'] + ' ' + s['title'] + '.docx', 'plannedPages': 3 + len(s['pages'])} for s in docs], ensure_ascii=False, indent=2), encoding='utf-8')
print("All deliverables generated successfully!")
