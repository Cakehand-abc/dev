# 项目进度与下次继续入口

更新：2026-09-09。下次使用时先向用户说明本文件中的已完成功能，再继续未完成事项。

## 已完成代码

Spring Boot 3.5.16、Java 17 编译目标、Maven 3.9.16、MyBatis-Plus 3.5.17 与 MyBatis XML、Vue 3、Element Plus 2.14.5。数据模拟页实际按需使用 Element Plus；ECharts 6 仅为当前统计图实现，不是必选技术约束。实现登录与退出、首页、人口统计、健康统计、医生随访、腕表分布、圆形围栏、轨迹播放，并配套老人、医生、设备、账号维护。三角色权限、CSRF、电话脱敏、随访唯一完成、绑定历史、乱序定位、越界去重与人工处理已实现。

V1.1 已按“数据模拟页面 → 批量轨迹注入 → 受限删除 → 真实 GPS 接口”完成优化：管理员可生成单点、直线、圆形和围栏穿越轨迹；单批最多 500 点且整批预校验；仅无健康、随访、绑定、轨迹和围栏关系的误录老人可物理删除；设备通过独立密钥调用定位和心跳接口，密钥只保存 SHA-256 摘要并支持轮换。归档恢复不实现。

## 已验证

- Maven 28 项测试通过，本机 JVM 为 Zulu 25.0.1，release 17；已识别本机 IDEA 2024.3.4，未实测 JDK 17 或 IDEA 内置运行器。
- MySQL 8.0.46 隔离验收通过：全新结构 18 表；V1.0 的 16 表经 03 脚本升级后为 18 表，哨兵数据保留。
- V1.1 实际 HTTP 基础冒烟 16 项、新增写路径 5 项通过；Chrome 新增功能 4 项通过，含 390 像素窄屏。
- 101080 健康记录、100648 定位记录，20 并发约 60 秒，2664 请求 0 错误；四端点 P95 均 < 2 秒，测试专属数据已清理。
- 代码已可打包运行，本机演示地址 127.0.0.1:18080，私有账号配置在 .local/demo-access.txt；不要把凭据写进公开文档。

## 当前交付状态

- 前后端代码、18 表 SQL 结构、V1.0 升级脚本和独立虚构种子完成；V1.1 JAR 构建成功。
- 十份 Word 已按三创谷模板重排并完成 91 页逐页渲染复核；16 页答辩 PPT 已覆盖模板规定的项目介绍、团队、分工、技术、难点、功能和项目感受，并通过包结构、版式、字体、可编辑表格和图表检查。
- 架构说明书包含系统用例图、分层图、包图、两张领域类图、部署图和 GPS 入库顺序图；IDEA 可直接预览的 PlantUML 源文件和同源 PNG 已交付。
- 根 README、docs/README.md、完整接口目录和部署环境说明已完成。
- 最新 JAR 在隔离 MySQL 上完成 HTTP 16+5 项和 Chrome 4 项 V1.1 验收；原 V1.0 浏览器 12 项及窄屏设备详情证据继续保留。
- 独立 SQL 首次导入及非空表重复导入拒绝验证通过，测试专属表清理完成。
- 可转交组员的整套 V1.1 交付包已输出为 release/smart-care-v1.1.zip，含源码、SQL、文档、测试证据和可执行 JAR，不含私有凭据与依赖缓存；SHA-256 见 release/SHA256SUMS.txt。

## 下次先说明的已完成功能

已实现登录退出、首页、人口信息分析、健康检测分析、医生随访分析、腕表分布、电子围栏、轨迹回放八项功能，并附老人、医生、设备和账号管理。三角色、历史绑定、越界告警和回放交互已接入真实后端与 MySQL。管理员还有数据模拟、最多 500 点批量注入、受限删除和设备密钥轮换；设备可经独立密钥调用通用 GPS 定位与心跳接口。

## 留待团队补充

实际例会信息及人工验收签字仍需团队填写。成员姓名与学号已统一，本机 IDEA 版本为 2024.3.4；JDK 17 和 IDEA 内置 JUnit 未实测，当前验证 JVM 为 25、release 17。公开部署、真实设备、道路地图和备份恢复演练未执行。无需再次选择模板。

## 用户约定

不设独立 Agent 岗位；周虹宏任项目经理兼测试负责人，钟毓林任后端工程师，胡吉涵任后端兼数据工程师，李治宏任前端工程师。使用虚构数据与无道路底图的离线坐标画布。第 8 项文档改详细设计，第 11 项为部署环境说明。截止日期仍待团队补充。

用户 2026-09-08 要求持续工作，若额度耗尽或需要选择模板样式则先保存进度并关机。当前模板已选，不需再问。额度耗尽可能直接阻断工具执行，不能保证自动关机；已向用户说明此限制。每阶段更新此文件。不得因本文件自行定时关机或在未出现条件时关机。

## 本机验证环境

原 V1.0 隔离 MySQL 端口 13316 当前未运行；V1.1 验收使用临时目录独立实例（数据库 13317、应用 18081），结果见 docs/evidence。可运行 JAR 位于 target/dev-0.0.1-SNAPSHOT.jar。避免停止用户已有 8080 服务或其他 java/mysql 进程。重新打包前先停止本项目 JAR，否则 Windows 文件占用会阻止 repackage。

Word 与 PPT 模板已经选定。Word 采用三创谷灰色侧栏封面、文档信息、修订记录、目录和灰阶表格格式；PPT 模板只有内容要求页，因此保留 16:9 专业答辩版式并补齐规定内容。任务脚本和 QA 在 scripts 与 docs/.qa。最终交付在 docs/deliverables，QA 中间文件不交付。
