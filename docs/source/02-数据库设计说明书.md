# 数据库设计说明书
版本 V1.0　2026年9月8日

## 1 数据库概览

数据库 smart_care 使用 MySQL 8.0，包含 16 张表。database/01-schema.sql 为结构权威来源；首次在专用空库执行，重复执行会失败，不包含删除或覆盖已有数据库的逻辑。

| 分组 | 表与职责 |
| --- | --- |
| 身份和档案 | sys_user、region、elder、doctor |
| 健康与随访 | health_rule、health_record、followup_plan、followup_record |
| 设备与定位 | watch_device、device_binding、location_point |
| 围栏与审计 | geofence、geofence_member、geofence_state、geofence_alert、audit_log |

## 2 关系与生命周期

地区一对多老人；老人一对多健康记录和随访计划；医生一对多计划；计划与完成记录为一对零或一。设备和老人通过历史绑定建立关系，当前绑定分别唯一。定位点同时保留设备、绑定及老人外键。

围栏通过成员表关联老人。每名成员最多一条监测状态；状态指向当前活动事件，事件指向越界定位与处理账号。先建立状态和事件表，最后追加 active_alert_id 外键以处理循环引用。

业务主键为 BIGINT，JSON 响应中的 Long 以字符串输出，避免 JavaScript 精度损失。业务 DATETIME(6) 保存上海本地时间；地区、规则与演示数据初始化由应用控制。

## 3 系统账号 sys_user

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| username | VARCHAR(64) | 必填 唯一  | 登录名 |
| password_hash | VARCHAR(100) | 必填  | BCrypt 密文 |
| display_name | VARCHAR(80) | 必填  | 显示名 |
| role | VARCHAR(20) | 必填  | ADMIN OPERATOR ANALYST |
| enabled | BOOLEAN | 必填 默认 TRUE | 是否启用 |
| auth_version | INT | 必填 默认 0 | 认证版本 |

表级约束：CONSTRAINT ck_user_role CHECK (role IN ('ADMIN','OPERATOR','ANALYST'))

## 4 地区 region

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| code | VARCHAR(30) | 必填 唯一  | 业务编号 |
| name | VARCHAR(80) | 必填  | 名称 |
| parent_id | BIGINT | 可空  | 父地区主键 |

表级约束：FOREIGN KEY (parent_id) REFERENCES region(id)

## 5 老人档案 elder

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| code | VARCHAR(40) | 必填 唯一  | 业务编号 |
| name | VARCHAR(80) | 必填  | 名称 |
| gender | VARCHAR(10) | 必填  | MALE FEMALE UNKNOWN |
| birth_date | DATE | 可空  | 出生日期 可未知 |
| region_id | BIGINT | 必填  | 地区主键 |
| phone | VARCHAR(30) | 可空  | 电话 响应脱敏 |
| status | VARCHAR(20) | 必填 默认 'ACTIVE' | 业务状态 |
| version | INT | 必填 默认 0 | 并发版本 |

表级约束：FOREIGN KEY (region_id) REFERENCES region(id)；CHECK (gender IN ('MALE','FEMALE','UNKNOWN'))；CHECK (status IN ('ACTIVE','ARCHIVED'))

索引：ix_elder_region (region_id,status)

## 6 医生 doctor

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| code | VARCHAR(40) | 必填 唯一  | 业务编号 |
| name | VARCHAR(80) | 必填  | 名称 |
| department | VARCHAR(100) | 必填  | 科室 |
| phone | VARCHAR(30) | 可空  | 电话 响应脱敏 |
| enabled | BOOLEAN | 必填 默认 TRUE | 是否启用 |
| version | INT | 必填 默认 0 | 并发版本 |

## 7 教学阈值规则 health_rule

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| metric | VARCHAR(30) | 必填  | 检测指标名 |
| lower_bound | DOUBLE | 必填  | 下界 包含 |
| upper_bound | DOUBLE | 必填  | 上界 包含 |
| unit | VARCHAR(20) | 必填  | 单位 |
| version | INT | 必填  | 规则版本 |
| enabled | BOOLEAN | 必填  | 是否启用 |

表级约束：UNIQUE(metric,version)；CHECK (lower_bound < upper_bound)

## 8 健康检测记录 health_record

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| measured_at | DATETIME(6) | 必填  | 检测时间 |
| systolic | DOUBLE | 可空  | 收缩压 mmHg |
| diastolic | DOUBLE | 可空  | 舒张压 mmHg |
| heart_rate | DOUBLE | 可空  | 心率 次每分 |
| oxygen | DOUBLE | 可空  | 血氧百分比 |
| temperature | DOUBLE | 可空  | 体温 摄氏度 |
| abnormal | BOOLEAN | 必填  | 是否超教学阈值 |
| rule_snapshot | TEXT | 必填  | 判定规则 JSON 快照 |
| source | VARCHAR(30) | 必填  | 数据来源 |
| event_id | VARCHAR(100) | 必填  | 外部事件编号 |

表级约束：FOREIGN KEY (elder_id) REFERENCES elder(id)；UNIQUE(source,event_id)

索引：ix_health_elder_time (elder_id,measured_at)；ix_health_time_abnormal (measured_at,abnormal)

## 9 随访计划 followup_plan

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| doctor_id | BIGINT | 必填  | 医生主键 |
| due_at | DATETIME(6) | 必填  | 计划到期时间 |
| status | VARCHAR(20) | 必填 默认 'PENDING' | 业务状态 |
| canceled_at | DATETIME(6) | 可空  | 取消时间 |
| cancel_reason | VARCHAR(500) | 可空  | 取消原因 |
| version | INT | 必填 默认 0 | 并发版本 |

表级约束：FOREIGN KEY (elder_id) REFERENCES elder(id)；FOREIGN KEY (doctor_id) REFERENCES doctor(id)；CHECK (status IN ('PENDING','COMPLETED','CANCELED'))

索引：ix_plan_doctor_due (doctor_id,due_at)；ix_plan_elder_due (elder_id,due_at)

## 10 随访结果 followup_record

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| plan_id | BIGINT | 必填 唯一  | 随访计划主键 唯一 |
| completed_at | DATETIME(6) | 必填  | 完成时间 |
| content | VARCHAR(2000) | 必填  | 随访内容 |
| result | VARCHAR(500) | 必填  | 结果摘要 |

表级约束：FOREIGN KEY (plan_id) REFERENCES followup_plan(id)

索引：ix_followup_completed (completed_at)

## 11 腕表设备 watch_device

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| serial_no | VARCHAR(80) | 必填 唯一  | 设备序列号 |
| model | VARCHAR(80) | 必填  | 设备型号 |
| enabled | BOOLEAN | 必填 默认 TRUE | 是否启用 |
| last_seen_at | DATETIME(6) | 可空  | 最后心跳 |
| version | INT | 必填 默认 0 | 并发版本 |

## 12 历史绑定 device_binding

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| device_id | BIGINT | 必填  | 设备主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| bound_at | DATETIME(6) | 必填  | 绑定开始时间 |
| unbound_at | DATETIME(6) | 可空  | 解绑时间 |
| active_device_id | BIGINT | 可空 唯一  | 有效设备唯一占位 |
| active_elder_id | BIGINT | 可空 唯一  | 有效老人唯一占位 |

表级约束：FOREIGN KEY (device_id) REFERENCES watch_device(id)；FOREIGN KEY (elder_id) REFERENCES elder(id)；CHECK ((unbound_at IS NULL AND active_device_id IS NOT NULL AND active_elder_id IS NOT NULL AND active_device_id = device_id AND active_elder_id = elder_id) OR (unbound_at IS NOT NULL AND active_device_id IS NULL AND active_elder_id IS NULL))；CHECK (unbound_at IS NULL OR unbound_at >= bound_at)

索引：ix_binding_history (device_id,bound_at,unbound_at)

## 13 定位点 location_point

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| device_id | BIGINT | 必填  | 设备主键 |
| binding_id | BIGINT | 必填  | 发生时绑定主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| event_id | VARCHAR(100) | 必填  | 外部事件编号 |
| longitude | DECIMAL(10,7) | 必填  | WGS84 经度 |
| latitude | DECIMAL(10,7) | 必填  | WGS84 纬度 |
| recorded_at | DATETIME(6) | 必填  | 设备记录时间 |
| received_at | DATETIME(6) | 必填  | 服务端接收时间 |

表级约束：FOREIGN KEY (device_id) REFERENCES watch_device(id)；FOREIGN KEY (binding_id) REFERENCES device_binding(id)；FOREIGN KEY (elder_id) REFERENCES elder(id)；UNIQUE(device_id,event_id)；CHECK (longitude BETWEEN -180 AND 180)；CHECK (latitude BETWEEN -90 AND 90)

索引：ix_location_elder_time (elder_id,recorded_at,id)；ix_location_device_time (device_id,recorded_at,id)

## 14 圆形围栏 geofence

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| name | VARCHAR(80) | 必填  | 名称 |
| center_lon | DECIMAL(10,7) | 必填  | 围栏中心经度 |
| center_lat | DECIMAL(10,7) | 必填  | 围栏中心纬度 |
| radius_m | DOUBLE | 必填  | 半径 米 |
| enabled | BOOLEAN | 必填 默认 TRUE | 是否启用 |
| version | INT | 必填 默认 0 | 并发版本 |

表级约束：CHECK (radius_m BETWEEN 50 AND 5000)；CHECK (center_lon BETWEEN -180 AND 180)；CHECK (center_lat BETWEEN -90 AND 90)

## 15 围栏成员 geofence_member

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| fence_id | BIGINT | 必填  | 围栏主键 |
| elder_id | BIGINT | 必填  | 老人主键 |
| enabled | BOOLEAN | 必填  | 是否启用 |

表级约束：FOREIGN KEY (fence_id) REFERENCES geofence(id)；FOREIGN KEY (elder_id) REFERENCES elder(id)；UNIQUE(fence_id,elder_id)

## 16 围栏监测状态 geofence_state

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| member_id | BIGINT | 必填 唯一  | 围栏成员主键 |
| last_point_id | BIGINT | 可空  | 最后参与监测的定位 |
| last_recorded_at | DATETIME(6) | 可空  | 监测事件时间 |
| inside | BOOLEAN | 可空  | 是否在围栏内 可未知 |
| active_alert_id | BIGINT | 可空  | 当前未结束越界事件 |

表级约束：FOREIGN KEY (member_id) REFERENCES geofence_member(id)；FOREIGN KEY (last_point_id) REFERENCES location_point(id)

## 17 越界事件 geofence_alert

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| member_id | BIGINT | 必填  | 围栏成员主键 |
| exit_point_id | BIGINT | 必填  | 触发定位点 |
| triggered_at | DATETIME(6) | 必填  | 越界触发时间 |
| returned_at | DATETIME(6) | 可空  | 实际返回时间 |
| closed_at | DATETIME(6) | 可空  | 监测关闭时间 |
| close_reason | VARCHAR(30) | 可空  | 关闭原因 |
| handled_by | BIGINT | 可空  | 处理账号 |
| handled_at | DATETIME(6) | 可空  | 处理时间 |
| handling_note | VARCHAR(1000) | 可空  | 处理说明 |
| geometry_snapshot | TEXT | 必填  | 围栏几何 JSON 快照 |
| version | INT | 必填 默认 0 | 并发版本 |

表级约束：FOREIGN KEY (member_id) REFERENCES geofence_member(id)；FOREIGN KEY (exit_point_id) REFERENCES location_point(id)；FOREIGN KEY (handled_by) REFERENCES sys_user(id)

索引：ix_alert_member_time (member_id,triggered_at)；ix_alert_handled (handled_at)

## 18 操作审计 audit_log

| 字段 | 类型 | 约束与默认 | 含义 |
| --- | --- | --- | --- |
| id | BIGINT | 必填 主键 自增 | 自增主键 |
| user_id | BIGINT | 可空  | 操作账号 可系统执行 |
| action | VARCHAR(60) | 必填  | 动作编码 |
| target_type | VARCHAR(60) | 必填  | 对象类型 |
| target_id | BIGINT | 可空  | 对象主键 |
| occurred_at | DATETIME(6) | 必填  | 操作时间 |
| result | VARCHAR(40) | 必填  | 执行结果码 |

表级约束：FOREIGN KEY (user_id) REFERENCES sys_user(id)

索引：ix_audit_user_time (user_id,occurred_at)

## 19 索引与完整性设计

| 约束 | 作用 |
| --- | --- |
| source 与 event_id 唯一 | 健康重复录入产生冲突，客户端可识别重试 |
| device_id 与 event_id 唯一 | 定位重传不重复入库 |
| active_device_id 和 active_elder_id 分别唯一 | 一个设备和老人同时最多一条有效绑定，历史解绑后置空 |
| followup_record.plan_id 唯一 | 防止同一随访计划重复完成 |
| 围栏成员及状态唯一 | 防止同一关联存在多条监测状态 |

归档以状态变更保留历史，不级联删除。定位、告警和绑定属于历史事实；修改围栏保存新的几何状态并关闭原监测事件，旧事件保留几何快照。

## 20 初始化 备份与迁移

先创建专用数据库，再执行表结构，再由 Bootstrap 建管理员、地区与教学规则。demo 模式且 APP_SEED_DEMO=true 时，空老人表触发演示种子；非空库不会重复生成。独立 SQL 种子见 database/02-demo-data.sql，与 Java 种子二选一。

结构调整应新增迁移脚本并先备份验证。当前项目没有自动 Flyway 迁移，也没有自动备份任务。导出与恢复命令见部署说明；生产恢复必须先在独立库验证，不覆盖现有业务库。
